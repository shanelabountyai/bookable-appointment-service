# Next

**A-094 — the mobile public booking flow (§8.8).** Next ⬜ in
`docs/prds/06-backlog.md`. The client's one shot, on a phone, at night, and she
will not see it again for six weeks. The public palette stays as it is (§9) —
this is composition and density, not a re-identity. Never leak *why* a slot is
unavailable (`explain` is staff-only, enforced at the route).

A-093 is committed and CI-green. The design pass still has A-096 (dark-scheme
axe across the rest of the staff app) riding along behind these, and A-095 and
A-097 are Phase 10 rows waiting.

Read `docs/START-HERE.md` and `CLAUDE.md` first, as always.
