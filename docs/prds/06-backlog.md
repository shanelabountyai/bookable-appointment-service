# 06 — Prioritized Product Backlog

**Product:** Bookable — appointment scheduling for a service business
**Status:** v1.0 — 2026-08-14
**Sources:** `00-master-prd.md` (story IDs are the authoritative acceptance criteria), `07-decisions.md` (overrides PRD text), `docs/reviews/03-slot-engine-spec.md` (normative for A-003/A-009/A-010)

Single, strictly-ordered build backlog: every item is buildable when reached, foundation first, shortest path to the two golden paths — **(1) a customer books a real slot against a race-proof engine and gets a confirmation with a working manage link** (end of Milestone 2) and **(2) the front desk runs a full Saturday from one screen: books a walk-in, checks a client in, pushes a late column, and rescues a sick stylist's day without one silent cancellation** (end of Milestone 3).

**Numbering.** `A-` numbers are global, continuous, permanent — never renumbered, never reused (`B-` = storage, `R-` = rental). Rows added later carry a letter (`16a`). Sizes: **S** ≈ short session, **M** ≈ one focused session, **L** ≈ 2–3 sessions. ✅ = built and committed; ⬜ = scoped, not started (so `grep ⬜` answers "what is next" — it returned nothing at the A-045 session's start, which is how an empty backlog looked). One item per session, top to bottom; the loop is in `docs/START-HERE.md`.

---

## Milestone 1: Foundation

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 1 | ✅ A-000 | PRD set, decision log, reviews, this backlog, CLAUDE.md, failing slot-engine spec suite — delivered as the project starter | — | — | — | MVP |
| 2 | ✅ A-001 | Monorepo & app scaffold: Next.js (App Router) + TS, Tailwind/shadcn, Prisma + **Postgres** (docker-compose; port **3300** as config default), Vitest/Playwright + axe, CI (migrations-from-scratch + drift check + suite under `TZ=Pacific/Kiritimati` AND `TZ=UTC`), the gate command, `.env` handling | D-1, D-3 | M | A-000 | MVP |
| 3 | ✅ A-002 | **`packages/core/time`**: `CalendarDay`/`WallTime`/`Instant` branded types, the single conversion module with three-armed `resolve()`, injectable clock, lint bans on axis-crossing syntax. TDD against spec §§1–3 (facts, DST resolution cases). The starter's `types.ts` is the contract | D-3; SLOT-02 | M | A-001 | MVP |
| 4 | ✅ A-003 | Core data model & migrations: every §8 entity, **the exclusion constraint in hand-written SQL**, stored `blockedStart`/`blockedEnd`, half-open ranges, append-only `AppointmentEvent` trigger, D-12 schema affordances (`ServiceSegment`, `ResourceType`, `AppointmentServiceLine`), `businessId` everywhere, no `payment_status`. **Before writing the schema, show the owner the entity list + constraint SQL and wait for confirmation.** **Per D-15..D-21:** constraint predicate `status NOT IN ('cancelled','cancelled_late')` derived from the status module; `overriddenFromRange tstzrange` (NOT NULL exactly for overrides); `Client.phone` indexed **not unique**, name/phone/email nullable; `Appointment.clientId` nullable; `AppointmentServiceLine.priceCents`/`durationMinutes` snapshotted; the three `SlotPolicy` flags + `bookingHorizonDays` on `Business`; `Service.cancellationCutoffMinutes` nullable; `Provider.active`/`displayOrder`; `Appointment.idempotencyKey` unique; `Appointment.startDay CHAR(10)`/`startWallTime CHAR(5)`; `WaitlistEntry.status`; CHECK that all four instants land on whole minutes. Constraint tests write directly against the database, bypassing the app, and include **both** a `cancelled` and a `cancelled_late` row failing to block a rebooking of the identical range | §6, §8; D-2, D-12, D-13, **D-15..D-21** | L | A-002 | MVP |
| 5 | ✅ A-004 | Notification outbox + `ChannelAdapter` with logging adapter; idempotent enqueue on dedupe key; kill switch + sandbox redirect. Built before its first sender | NOTIF-01; D-14 | M | A-003 | MVP |
| 6 | ✅ A-005 | Staff session (single credential) + `actor` stamped on every mutation; staff routes refuse unauthenticated requests | D-9 | S | A-001 | MVP |

## Milestone 2: The booking loop (Golden Path 1)

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 7 | ✅ A-025 | **Business & provider setup**: owner settings (timezone, slot interval, lead time, cutoff, no-show threshold, `bookingHorizonDays`, the three `SlotPolicy` flags), provider CRUD with `active` + `displayOrder`, provider deactivation writing `Provider.active` and the call site only (**the AVAIL-05 impact preview itself lands in A-019** — no appointment can exist until A-009, so a preview here would ship untested against an empty set; operator S-2). **Also builds the SETUP seed** — business, 4 providers, 8 services with unequal buffers, weekly windows, one date override — so A-006/A-007/A-026 are developed against a real roster rather than three hand-typed rows (operator S-1). Roster must never exceed the chair count (D-20). **Validates `minimumLeadMinutes >= max(business.cutoff, every active service.cutoff)`** on the settings form and on service save, not at startup only — D-11's trap re-opens through D-19's per-service cutoff otherwise (operator R-3). *Nothing in the backlog created a `Provider` or a `Business` before this row — the seed made them, a paying customer cannot* | §8; D-9, D-11, D-19, D-21; SVC-03 (provider half) | M | A-003, A-005 | MVP |
| 8 | ✅ A-008 | **Slot engine (PURE)** — the core learning artifact. `computeSlots(SlotQuery) → SlotResult` per the starter's `types.ts`. Make the pre-written red suite (`slot-engine.test.ts`) green, then extend to the full spec §3 matrix + §2 invariants incl. fast-check property tests. **No database, no adapter, no `daysWithAvailability`** — the query takes plain data, so this needs nothing from A-006/A-007. Definition of done = the matrix under `TZ=UTC` AND `TZ=Pacific/Kiritimati`, not the PRD sentence | SLOT-01..06, 08 | L | A-002, A-003 | MVP |
| 9 | ✅ A-006 | Service catalog: CRUD, per-provider qualification with duration/price overrides, deactivation-with-future-appointments flow | SVC-01..03 | M | A-003, A-005 | MVP |
| 10 | ✅ A-007 | Availability model: weekly windows (+breaks as children, `endsNextDay`), `DateOverride` parent with `isClosed`, time off / ad-hoc blocks as instant intervals, business hours ∩ provider hours, the fixed precedence chain. **`createdByActor`/`actorRef` on TimeOff, AdHocBlock, WeeklyWindow and DateOverride** — `AppointmentEvent` covers appointments and stops at the appointment boundary, so "who blocked Dana's 2-4 and why?" has no answer today (operator R-8) | AVAIL-01..04 | L | A-003, A-002 | MVP |
| 11 | ✅ A-026 | **Availability → `SlotQuery` adapter**: the busy-set query (instant-overlap predicate, `COALESCE("overriddenFromRange", tstzrange(...))` per D-16 — never `WHERE date(startAt) = day`), window resolution from A-007's precedence chain, `daysWithAvailability(provider, service, month)`, `bookingHorizonDays` cap on self-serve only | SLOT-07; D-16, D-20, D-21 | M | A-006, A-007, A-008 | MVP |
| 12 | ✅ A-009 | Booking write path: transaction re-runs engine, `23P01 → SlotTaken → 409` with refreshed alternatives, idempotency key, confirmation via outbox with manage-link placeholder; **all eight deterministic race tests** (spec §4.5's eight interleavings) **plus a ninth: staff override vs. concurrent self-serve booking of the same range** — an override's blocked range is zero-width, so the database no longer defends that time and only the in-transaction re-check does (operator R-9) — + the nightly SQL-invariant fuzz. **Signature is STAFF-shaped from day one** (operator S-3): nullable client, uncapped horizon, `isOverride` + reason, and `actor`; self-serve is the RESTRICTED caller, not the other way round — reopening this at A-017 would reopen the manage token, the outbox enqueue and the race tests with it | BOOK-02, 03, 06; D-2 | M | A-008, A-026, A-004 | MVP |
| 13 | ✅ A-028 | **Multi-service visit (VISIT-01, D-23)**: one appointment, one provider, several ordered services. Durations compose into one body; buffers do NOT stack between lines (first line's `bufferBefore`, last line's `bufferAfter` only). One `AppointmentServiceLine` per service, each snapshotting price/duration (D-18). Extends A-009's write path while it is fresh; the engine needs no change — a composed visit is just a longer service. *Half the sample salon's Saturday is cut+colour, and the two-adjacent-appointments workaround is refused by the database* (operator R-5) | VISIT-01; D-23, D-12 | M | A-009 | MVP |
| 14 | ✅ A-011 | **Density seed** per §9 spec: appointments against the A-025 setup seed — fixed anchor spanning both DST transitions, per-provider density targets (one ~85% booked, one ~40%, one fully booked 3 consecutive days, one split-shift with mid-window time off), ≥1 client with no-show history, deterministic. **Moved ahead of A-010** so the customer UI is built against a realistic book — a full day, a day with one slot left, the doubled fall-back hour — instead of an empty calendar where every screen looks fine (operator S-1) | §9 | S | A-009 | MVP |
| 15 | ✅ A-010 | Customer booking flow UI: ≤5 screens / ≤3 required inputs, phone-first identity, instant-keyed slot POST, WCAG 2.1 AA incl. keyboard slot grid + live region (axe in the spec) | BOOK-01; D-4, D-10 | M | A-009 | MVP |

> **✅ Demo checkpoint 1 — walked 2026-08-16, and it found one real defect** (every outbox row orphaned from its appointment). Full transcript and findings: `docs/reviews/06-demo-checkpoint-1.md`.
>
> **Demo checkpoint 1.** Against the seed: a customer picks a colour with Dana, sees Tuesday's slots with the 12–1 break absent and 11:15 first after the 10:00 booking, books, and the outbox holds one confirmation with a manage link. The scripted race for an *overlapping non-identical* slot yields exactly one winner and a 409 with alternatives. A direct SQL insert of an overlapping appointment is refused by the database. The suite passes under `TZ=Pacific/Kiritimati`. **Walk it when the milestone closes, not when convenient** — rental D-28: all four of its checkpoint-1 defects were invisible from inside the items that introduced them.

## Milestone 3: Running the day (Golden Path 2)

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 16 | ✅ A-012 | Appointment state machine: 8 states, the §7 table as one parameterised test over all pairs, terminal corrections (staff, ≤7d, reason), busy-set membership per D-7, append-only event log wired to every transition | APPT-01, 05, 06, 07; D-7 | M | A-009, A-005 | MVP |
| 17 | ✅ A-013 | Manage token: scoped, multi-use to `end + 24h`, revoke-on-reissue, rate-limited, lexicon test (no internal identifiers) | TOKEN-01..03; D-5, D-10 | M | A-012, A-004 | MVP |
| 18 | ✅ A-014 | Reschedule: same-row update in one transaction, engine re-run inside, both-sides event, cutoff applies to token actor, token re-points | APPT-05; D-6, D-11 | M | A-013 | MVP |
| 19 | ✅ A-015 | Client record: phone-keyed, merge-with-history, client notes (pinned) + **per-visit appointment notes** (`Appointment.notes`, added at the M1 boundary — operator R-6), history view, "rebook last visit" , **`mergedIntoClientId` tombstone** so the losing record's phone still finds the survivor (operator R-10) | CLIENT-01..03 | M | A-012 | MVP |
| 20 | ✅ A-016 | Staff day grid: column per provider, time gutter, now-line, status colours, clickable gaps with lengths, client chips (phone, flags, pinned note), 30s-bound staleness (e2e-tested), single-provider list view for a stylist's own day; **WCAG 2.1 AA including full keyboard operability of the grid + axe in the spec** (the front desk types faster than it mouses) | BOOK-04 (view half); Goal 3 | L | A-012, A-015, A-007 | MVP |
| 21 | ✅ A-027 | **Appointment detail panel**: the plain-language event log (APPT-07), pinned client notes on every render (CLIENT-03), override marker + reason (BOOK-05), per-appointment outbox history ("was she actually told?" — one indexed lookup on `NotificationOutbox.appointmentId`, added at the M1 boundary; operator R-4), status controls, conflict marker. *Four stories require this screen and no row built it* | APPT-07; CLIENT-03; BOOK-05; D-8 | M | A-012, A-015, A-016 | MVP |
| 22 | ✅ A-017 | Staff booking & override: from-the-grid booking (**including making A-016's day-grid gaps clickable** — deferred from A-016 with the owner's agreement, because a gap button had nowhere to go until this row exists; two lines in `day-grid.tsx`), partial phone/name search, walk-in-now, no-client booking, override paths (outside hours / into buffer / knowing double-book) each with reason + audit + zero-width-range mechanics | BOOK-04, 05; D-8 | M | A-016 | MVP |
| 23 | ✅ A-018 | Check-in & running late: `checked_in`/`in_progress` with actual timestamps, per-column running delta, projected starts, "push column from here" with collision preview + outbox notice; **stores the running-late delta per provider per business day (D-22)** as a value with actor + timestamp, settable and clearable in one tap, NOT a rewrite of `startAt`; the adapter feeds it to the engine as a `running-late` BusyInterval, and "push the column" stays the separate audited action that DOES rewrite `startAt`. **Multi-row moves run in ONE transaction with `SET CONSTRAINTS appointment_no_overlap DEFERRED`** (made deferrable at the M1 boundary; operator R-2). Same-day slot computation consumes the delta | APPT-03, 04 | M | A-016, A-012 | MVP |
| 24 | ✅ A-019 | Availability-change impact workflow: preview conflicts on any hours edit / time off / block / deactivation, per-appointment actions, bulk "reassign Saturday to Priya where qualified," conflict markers, nothing silent. **Conflicts stay DERIVED; only the human acknowledgment is stored** (`conflictAckAt`/`conflictAckReason`, added at the M1 boundary), cleared when the overlapping absence changes — otherwise the second person on Saturday morning re-rings three clients already sorted (operator R-7). **Also owns the provider-deactivation impact preview** moved out of A-025 (operator S-2) | AVAIL-05 | M | A-016, A-007 | MVP |

> **Demo checkpoint 2.** Saturday, seeded: front desk books a walk-in from the grid in the 2:15 gap; checks in the 10:00 who arrived at 10:12; Dana runs +38 and the column shows projected starts; "push from 2pm" previews the 4:30 collision with close and staff resolves it; Dana calls in sick → time off entered → nine conflicts listed with phones → three reassigned to Priya, six kept-flagged; a customer opens the manage link, reschedules, then cancels with the same link; a `no_show` mis-tap is corrected to `completed` with a reason; every one of those actions is in the event log in plain language.

## Milestone 4: The revenue leak

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 25 | ✅ A-020 | No-show & late-cancel machinery: `cancelled_late` split enforced at the cutoff, rolling 12-month client counts with references, flags on every client surface, self-serve block after N with staff bypass | CLIENT-04; OQ-3 | M | A-015, A-012 | MVP |
| 26 | ✅ A-021 | Confirm loop: confirm via manage link and staff manual-confirm, `confirmed_at`, unconfirmed-tomorrow call-down view; no auto-cancel ever | APPT-02 | S | A-013, A-016 | MVP |
| 27 | ✅ A-022 | Reminder job: 24h-window instant arithmetic, exactly-once, idempotent, skips terminal/rescheduled-away, DST-window test, confirm/cancel actions on the link | NOTIF-02, 03 | M | A-004, A-013, A-011 | MVP |
| 28 | ✅ A-023 | Waitlist, staff half: entries (service/providers/range/day-parts), fit-aware matching against a freed interval, "who wants this slot?" panel on any freed slot. Automated offers gated on OQ-4 | WAIT-01, 02 | M | A-020 | MVP |
| 29 | ✅ A-024 | Owner dashboard: bookings, cancels split normal/late, no-shows by provider, utilization to the frozen RPT-02 formula (exact seeded constant asserted), every tile drills to a filtered list | RPT-01..03 | M | A-011, A-020 | MVP |

> **Demo checkpoint 3.** The reminder run against the seed produces exactly one outbox row per eligible appointment and zero for the cancelled ones; re-running it adds nothing. A 2pm cancellation lights the waitlist panel with the two entries that actually fit. The dashboard's utilization for provider P week W equals the frozen constant, and the late-cancel tile drills to the two seeded offenders.

## Phase 3

### Scoped

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 30 | ✅ A-029 | **Segments modeled and visible** — the half that needs no constraint change. `ServiceSegment` stops being an affordance: the one-active-segment unique index is dropped for a `(serviceId, ordinal)` ordering with `status` filtering, the service form edits an ordered segment list with `isGap`, and a CHECK-or-test asserts `Service.durationMinutes` equals the sum of active segments so the footprint the exclusion constraint already ranges over cannot drift from the parts. Per-provider overrides scale active segments only, gap held fixed, refused at save if any active segment would reach ≤ 0 (SEG-02). Day grid and appointment detail render the gap as a distinct stretch with its length legible (SEG-03). **The engine is untouched and the database still blocks the whole footprint** — the desk can see the 35 free minutes and book them through the existing BOOK-05 override, which is what the operator called a defensible v1 ("booking into the gap manually first"). Seed gets one three-segment colour service so the stripe is visible without hand-typing rows | SEG-01..03; D-12 | M | A-006, A-016, A-027 | Phase 3 |
| 31 | ✅ A-030 | **Gap booking** — the engine offers the gap, and the database defends it. `computeSlots` consumes provider time from active segments only (SEG-04), so a candidate landing entirely inside another appointment's gap is a real slot rather than an override. **This is a constraint migration, and the reason it needs one is worth stating plainly: `appointment_no_overlap` ranges over ONE `tstzrange` per `Appointment` row, and a range cannot express a hole.** Once an appointment's provider-occupancy is a *set* of spans, the constraint's unit stops being the appointment — see OQ-7, which gates this row. Everything that writes or moves an appointment range moves with it: A-009's eight race interleavings plus the ninth, D-16's zero-width override range, A-018's `SET CONSTRAINTS ... DEFERRED` column push, A-019's bulk reassign, and A-026's busy-set query. **Do not start this row before OQ-7 is answered** — the answer decides the shape of every write path listed | SEG-04, 05; D-2, D-12 | L | A-029, **D-29** (answered OQ-7) | Phase 3 |

| 32 | ✅ A-031 | **Resource pools** — the axis D-20 ruled out of v1, which **A-030 has now made load-bearing**. `ResourceType`/`Resource` stop being affordances: a type gets its resources, a service declares what it needs, and an appointment holds one for its whole envelope INCLUDING its gaps (RES-02) — provider occupancy and chair occupancy are different sets, and that difference is the epic. Enforcement is the database, as D-2 requires: with N named resources the cardinality question "at most N clients at once" becomes N ordinary overlap questions, which an exclusion constraint can express — see OQ-8, which gates the shape. `AppointmentBlock` already exists and is the natural carrier, so the expensive half of this was paid for by A-030. Staff override per RES-04. **Also replaces the setup seed's `CHAIR_COUNT` guard**, which asserts the roster does not exceed the chairs and is now insufficient for the reason this row exists | RES-01..05; D-12, D-20, D-29 | M | A-030, **D-30** (answered OQ-8) | Phase 3 |

> **Why this stopped being optional.** D-20 ruled the resource axis out of v1 on one premise: *"for a 4-chair salon with 4 stylists the pool never binds, so the axis would be enforcement theatre."* That was true and is no longer. **A-030 exists precisely so a client can occupy a chair while her provider is free to work on someone else** — the passing e2e in `apps/web/e2e/segments.spec.ts` books a second client into Dana's column while the first is developing, which is two chairs for one stylist. Four stylists doing that is eight clients in four chairs, and the system accepts every one of them. The seed's `CHAIR_COUNT` assertion still guards the old premise (roster ≤ chairs) and cannot see the new one. **This is a consequence of shipped work, not a new feature request** — the software now permits something the room physically cannot do.

### Phase 3, second pass — the operator review at the Phase 3 boundary

> Written 2026-08-20 from `docs/reviews/08-operator-review-phase-3.md`, walked when the scoped Phase 3 rows closed and the backlog ran out. **Its finding is a pattern, not five unrelated gaps:** three mechanisms were built correctly at the write path and each deferred its operator-facing half to the next row, which deferred it again. Every row below closes one of those loops, and none of them is a new feature.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 33 | ✅ A-032 | **The room is visible to the engine, and a full room is never a crash** (operator P-2). `NoResourceFree` escaped both action layers, so the customer offered 11:15 in a full room saw a stack trace and staff could not reach RES-04's override — which is only offered on `SlotTaken`/`SlotNotOffered`. Deeper half: `packages/db/scheduling/` referenced no resource at all, so a full room removed no candidates and the refusal could only ever arrive at submit — the offered-then-refused class this repo has caught twice already. Adds the `resource-full` busy kind and the `no-resource-free` exclusion reason, derived by the adapter the way D-22's overrun is, so **the pure engine still never learns what a chair is** | RES-03, 04; D-30 | M | A-031 | Phase 3 |
| 34 | ✅ A-033 | **Staff reschedule** (operator P-1, THE most consequential gap). `rescheduleAppointment` has one caller and it is the customer's manage link, so the desk's only way to move a 3 o'clock to 4 is cancel-and-rebook — which A-012 correctly records as `cancelled_late` against a client who did nothing wrong, on five surfaces and in the owner's tile. A move control on the detail panel and the day-grid chip over the EXISTING write path and `rescheduleOptions`; staff unrestricted (no lead time, no horizon, no cutoff) with the BOOK-05 override on refusal; **provider change in the same action**, because "put her with Priya at 2 instead of 3" is one operation to a human; A-019's "offer a new time" repoints here. Regression test: a staff move produces a `rescheduled` event and NO `cancelled_late`, and the manage token still resolves. **Shipped WITHOUT the provider change and WITHOUT an override** — both are documented in `reschedule.ts` as deliberately absent, and reopening a settled write-path decision mid-item is exactly what OQ-7 and OQ-8 established should not happen; they are now **OQ-9** | APPT-05; D-6, D-25 | M | A-027, A-019 | Phase 3 |
| 35 | ✅ A-034 | **The chair follows the move** (operator P-3). `rescheduleAppointment`, `pushColumn` and `reassignAppointment` all carry `resourceId` forward unchanged, so a pushed column can collide on the resource axis at COMMIT — and `push-column.ts` maps no `isSlotTakenError` at all, so the desk gets a raw database error mid-push, on the promise of a preview that could not see it coming. All three re-run `findFreeResource` for the destination in the same transaction; `previewPush` runs the same check the action executes (A-018's own rule); a blocked appointment comes back through D-26's `leftBehind`, never as an exception; a reschedule with no free chair returns `NoResourceFree`, not `SlotTaken`. **First test to write is the fixture that proves the collision is reachable** against the seeded Saturday | RES-03; D-26, D-30 | M | A-032, A-033 | Phase 3 |
| 36 | ✅ A-035 | **Status controls on the day-grid chip** (operator P-4). Check-in costs four interactions and two page loads today, for the most frequent action in the salon; `provider-day.tsx` renders "Checked in" as a label with no `changeStatus` call. Check in / start / complete / no-show on the chip, same `expectedFrom` optimistic lock A-027 uses, buttons derived from the §7 table asked with the real actor and clock, keyboard-reachable, chip still links through. **Reuses `StatusControls`' logic rather than forking it** — two surfaces disagreeing about what is allowed is the failure mode. No revenue attaches to this, which is why it keeps being deferred; it is the adoption gap, and paper comes back out for check-ins first | APPT-03; Goal 3 | S | A-016, A-027 | Phase 3 |
| 37 | ✅ A-036 | **Reassign and cancel tell the client** (operator P-5). Book, reschedule and push-the-column all enqueue an outbox row; bulk reassign and staff cancellation — the two that happen on the day a stylist calls in sick — enqueue nothing. Three clients silently moved to a different stylist is the silent change AVAIL-05 says never happens. One row per moved appointment inside the same transaction as the write, a cancellation notice carrying A-019's reason, both suppressible so a client already phoned gets no contradicting text, and the conflicts screen showing who has been told from `NotificationOutbox.appointmentId`. **Both halves built** — the owner's answer is **D-32**: the notice is ON by default and suppressible per action, because an opt-in tick would be unticked on the one morning the feature exists for | AVAIL-05; NOTIF-01 | S | A-019, A-004 | Phase 3 |
| 38 | ✅ A-038 | **Reschedule across providers** (answers OQ-9 → **D-31**; ordered next, ahead of A-034). One optional destination provider on `rescheduleAppointment`, taking the SAME single advisory lock on the destination provider-day — the source calendar is only vacated, so there is one lock and nothing to order, and spec §616's canonical-ordering warning is aimed at the two-lock `FOR UPDATE` design it appears beside. A-019's SVC-02 qualification rule reused, not restated. `provider_changed` + `rescheduled` written in ONE transaction so the log says both happened at once. A provider control on A-033's move panel. **The lock claim in D-31 was WRONG and building this row found it**: the exclusion constraint waits on an uncommitted conflicting row rather than failing fast, so a swap deadlocks (`40P01`, unmapped, a 500) — both provider-day locks are taken in canonical order, which also closes the same latent deadlock for a same-provider move across two days, reachable since A-014. Ordering asserted as a property, never as a live race. *The row exists because the sick-stylist move is not composable from reassign and reschedule: each fails on its intermediate state while the destination is free* | APPT-05; D-6, D-31; SVC-02 | M | A-033 | Phase 3 |
| 39 | ✅ A-037 | **Named staff identity** (operator, unscoped list, moved up). Every mutation is stamped `actor: 'staff'` from one shared credential, so the event log says "by the front desk" for all four people at the terminal — and the brief's rule is that "who moved this appointment and when" always has an answer. The CHEAP shape is the right one, and D-9 is explicit that the minimal credential was deliberate: a named-staff row and a PIN, `actorRef` carrying the id (the column already exists on the availability tables from R-8), the event log rendering the name. **Identity, not roles** — no RBAC matrix, no permissions model. **The owner's answer is D-33**: the PIN switches identity INSIDE an already-authenticated session rather than replacing the password, so the public sign-in door keeps its full strength and only the *switch* gets cheap; a roster identity may have no credential at all, and off-boarding deactivates rather than deletes, because deleting the row would strip the name off every event it ever stamped | D-9, **D-33**; APPT-07 | M | A-005 | Phase 3 |


### Phase 3, third pass — the operator review at the Phase 3 close

> Written 2026-08-21 from `docs/reviews/09-operator-review-phase-3-close.md`, walked when the second-pass rows (A-032..A-037) closed and the backlog ran out again. **Its finding is one pattern with four faces:** every write path in the build takes an arbitrary day and an arbitrary instant, and every staff *screen* hands it either today's grid or a slot the engine already offered — so the desk can only act on today, and only on the times the engine is willing to sell. The previous pass's pattern (mechanism shipped, operator surface deferred) is confirmed closed on all five of its instances. Two of the rows below are again *finishing* a row already marked ✅ rather than new capability, which is the trade the operator says to take every time.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 40 | ✅ A-039 | **The desk can reach a date** (operator P-6, ranked first and the one that costs money daily). `/staff/day` navigates by **Previous / Today / Next** and nothing else, so Mrs. Kerr's "same again in six weeks" is forty-two taps at the highest-conversion twenty seconds in the business — and the front desk writes it in a diary instead, which is where double-bookings come from. The mechanism is already there: `/staff/day?day=…` renders any day and `safeDay` treats a hand-typed one as ordinary input; there is simply no control that emits the URL. A native `<input type="date">` submitting `?day=` (exactly as `move-panel.tsx:74` already does) on the day grid AND on `/staff/book`, so the panel can change its own day. Second half, same wall: a client's upcoming appointment renders as **plain text** on her record (`clients/[id]/page.tsx:134-151`) while the call-down list one screen over links every row — so the desk can see the appointment it needs to move and cannot click it. History rows link to `/staff/appointments/{id}`, future separated from past under their own heading (`clientHistory` already returns both). **Acceptance is in the desk's terms**: from a client's record, book her next visit on a named date and move her existing one, each without touching the day grid. *This is A-033's other half — that row gave the **move** a date box and left the **booking** without one* | BOOK-04; CLIENT-01 | S | A-016, A-033 | Phase 3 |
| 41 | ✅ A-040 | **Rebook books what she actually had** (operator P-7, CORRECTNESS RISK). The one forward-booking path that exists is the wrong one: the client card names "Cut + Colour with Dana" and the button links to **`/book`** — the customer's flow — carrying `rebook.serviceIds[0]!`, so a three-hour cut-and-colour is booked as a forty-five-minute cut and nobody notices until she arrives. Three more defects follow from the destination being the public flow, all in `public-actions.ts`: **`audience: 'public'`** on the engine calls and the write, losing D-25's staff lead-time exemption and D-21's uncapped staff horizon on the surface staff use most; the **resolved `clientId` thrown away** and re-resolved by (phone, name), so "Jen" where the record says "Jennifer" splits her history, her notes and her no-show count — the exact harm D-17 and A-015's merge exist to contain; and **CLIENT-04's block applying**, so a flagged client standing at the counter is told to "please call the salon" *by* the salon, with the staff bypass unreachable. Point Rebook at the staff surface carrying `providerId`, **every** `serviceId` in order, `clientId` and `fromDay` — `staffSlotsFor`/`bookAsStaff` already take the ordered array. Regression test: a two-line rebook produces two `AppointmentServiceLine` rows in the original order on the SAME `clientId`, with no new `Client` row written | VISIT-01; CLIENT-01, 04; D-17, D-21, D-25 | S | A-039 | Phase 3 |
| 42 | ✅ A-041 | **Deactivation and absence say what they just stranded** (operator P-8 — the P-1 pattern again, and this row FINISHES A-019 rather than adding to it). `grep -rn "listDeactivationImpact" apps packages` returns **exactly one line: its own definition.** A-019 built the provider-deactivation impact preview that A-025 moved to it — `futureAppointments`, `listDeactivationImpact` — and wired it to nothing; `settings/actions.ts:94` still carries the comment saying the preview "is A-019's" and then calls `setProviderActive` and returns. Dana hands in her notice, the row goes grey, and forty booked appointments are now held by an inactive provider, invisible to the booking flow, surfacing only on a Conflicts screen nobody has been given a reason to open. Same gap smaller on the absence path: `addAbsence` returns a `FormState` of errors and `availability-client.tsx` renders no conflict list and no link to one — which survives a sick day only because the desk is already on the day grid, and fails for a resignation, a holiday entered in advance, or an hours change made at home on a Sunday. Deactivation becomes a two-step confirm over the list that function already returns, the same shape as SVC-03's existing `DeactivationRequiresConfirm` gate. **Nothing refuses** — recording that Dana is off must always succeed (D-2, A-007); this is the sentence that comes back, not the write. Regression test: deactivating without confirming leaves `Provider.active` unchanged, and confirming leaves every appointment untouched | AVAIL-05 | S | A-019, A-025 | Phase 3 |
| 43 | ✅ A-042 | **The override is reachable from a screen** (operator P-9). D-8 calls the knowing double-book the hardest-won point in the product, and the build honours it everywhere except where a human is: `isOverride` + reason, the zero-width blocked range, `override_booked`, the detail marker, D-24's advisory lock and the ninth race interleaving all exist and are tested — and the desk cannot get to any of it. `chosenSlot` is either a slot the engine offered or an `at` from the URL, the only links that emit an `at` are gap chips, and a gap is by construction *free* time. So the override checkbox appears only after a refusal the desk has no way to cause. **The proof is the test itself**: `staff-booking.spec.ts:149-158` reaches the override through a hand-built `?at=18:00`, which is not a URL this product ever produces — and "move her to 6pm, we'll stay late", the case A-038 deliberately routed BACK to an override booking, has the same problem. A time entry on the staff panel over the day's grid instants, not restricted to offered ones, composed to an instant server-side exactly as the gap link does (D-4 — the form carries the instant, never `{date, time}`). **Folds in A-032's deferred half**: show staff the excluded times WITH reasons — `explain` is already returned on `audience: 'staff'` and `readableReason` already exists — "10:00 — she already has a client" with Book-it-anyway beside it is the whole feature. Also fixes the heading naming a time the form is not booking when a short gap preselects a later slot. Regression test: fully booked column → staff picks an occupied time → refused in words → ticks override with a reason → zero-width range + `override_booked`, all from the browser with no hand-built URL | BOOK-05; D-4, D-8, D-24 | M | A-039, A-032 | Phase 3 |
| 44 | ✅ A-043 | **What's opened up** (operator P-10 — WAIT-02's missing entry point, NOT the automated-offer question, which stays with OQ-4). A client cancels next Thursday's colour through her manage link on Saturday. The notice is correctly staff-only (`transition.ts:206`) — she does not need telling what she just did — but nothing tells the *salon* either, and no screen shows a freed slot: the day grid draws the hole only on Thursday, and per P-6 the desk cannot easily get to Thursday. Three hours of the most valuable service in the book sit unsold for six days while the waitlist entry that fits sits in `/staff/waitlist`. The matching machinery is built and good and has **one door**: `matchFreedSlot` is reached only from URL params built on the cancelled appointment's own detail page — so "who wants this slot?" requires already knowing which appointment was cancelled, which is the one thing the desk does not know. A derived list of appointments moved into a slot-freeing status recently whose freed time is still future and still empty, **ordered by how soon the time expires** (a Thursday 2pm dies on Thursday at 2), one tap from the day grid beside Walk-in / Conflicts / Call-down with a count on it, each row linking into the `matchFreedSlot` URL the detail page already builds plus a `tel:` link. Derived on every read, nothing stored — A-019's conflicts and A-020's counters discipline. **First test is the fixture that proves the list is bounded**: `appointmentsInRange` has no lower time bound anywhere, which the operator proved from the query and did NOT prove by running the seed forward | WAIT-02 | M | A-023, A-039 | Phase 3 |
| 45 | ✅ A-044 | **The audit trail cannot be forged in thirty seconds** (operator, from the unscoped verdict — the one piece of "multi-user auth" that should come forward, and explicitly NOT roles). A-037's own left-behind records both halves: the roster screen is reachable by any staff member, so anybody at the counter can set anybody else's PIN — which makes the named identity the whole row exists for forgeable by the person most motivated to forge it — and nothing ever hands the desk back, so whoever tapped last stays until somebody else taps. **The fix is a guard, not a permissions matrix**: setting or resetting a PIN requires the password session (`sub`), never the acting identity (`act`); and `act` expires on a timeout, falling back to the account holder exactly as a deactivated actor already does. Third small item, same session because it is one sentence of copy: A-036's "Told: …" column means "we wrote an outbox row" and today that row is a console log — staff will read it as "she has been texted", so it says **queued** until a real driver exists (D-14). *Deliberately still no RBAC — D-9 and D-33 stand; every staff member can do exactly what a staff member could do before* | D-9, D-33; NOTIF-01 | S | A-037 | Phase 3 |

### Phase 3, close-out — the question demo checkpoint 3 left open

> Written 2026-08-22 from `docs/reviews/10-demo-checkpoint-3.md`. The checkpoint closed with one open question rather than a feature gap — *what else is only true the second time?* — and one **wrong** diagnosis of why the suite missed the dormant resource layer. Both are cheaper to settle now than after another sixteen items are built on the assumption.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 46 | ✅ A-045 | **What is only true the second time** (demo checkpoint 3's carried-forward question, answered by measurement rather than by inspection). Run the real seeds twice on a clean database with **no reset between** and diff every column of every table: any statement whose effect depends on rows created later in the same pass differs between run 1 and run 2 by construction, in whichever direction it is wrong. Make the diff a permanent test rather than a one-off audit — the chair defect lived in a COLUMN, and the seed's existing idempotence test counted providers and services, so no count could ever have seen it. Also settle the checkpoint's stated cause: it claimed "every e2e spec seeds on top of an already-seeded database", which is **not what happens** — `fixtures.ts` truncates before every test, so e2e has always exercised a first run | — | S | checkpoint 3 | Phase 3 |

### Phase 4, first pass — scoped at the Phase 3 close-out

> Written 2026-08-22, after A-045 emptied the backlog for the fourth time. **Not a salon-operator review** — the agent was unavailable this session, so this is a scoping pass over the accumulated `**Left behind:**` entries in `PROGRESS.md`, the eleven `ponytail:` ceilings in the source, and the two open questions still unanswered (OQ-4, OQ-5). Every claim below was verified against the code, not taken from the note that predicted it. **Run the operator agent before building A-048 or later** — the first two rows are consequences of work already shipped and do not need a front-desk opinion; the ordering after that does.
>
> **The organising fact of this phase:** demo checkpoint 3 switched the resource layer on. For four items chairs were modelled and dormant, so every gap around them cost nothing. They now bind in every environment, and the two rows below are what that turned from theoretical into daily.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 47 | ✅ A-046 | **The chair the desk cannot see** (the direct consequence of checkpoint 3, and the reason it ranks first). `grep -rn "hair" apps/web/app apps/web/components` returns **two strings, both of them refusals**: `column-controls.tsx:130`'s "stays: no chair free at the new time" and `reschedule-actions.ts:181`'s "Every Chair is taken at that time". There is no `/staff/resources`, no chair on the appointment detail, no chair row on the day grid, and no way in the product to add a fifth chair, retire one, or say that a Blow-dry does not need one — `requiredResourceTypeId` is written by the seed and by **nothing else**, so a real salon cannot model its own room at all. Until checkpoint 3 that was harmless, because no service ever asked for a chair and D-30's constraint guarded an empty table. It is now the live path: the desk is refused a booking, and a push is told a client "stays", **on the authority of a resource the product has never once shown them**. A refusal naming a thing the operator cannot see or change is the complaint that ends with the salon booking on paper. Scope: chairs visible where they bind (detail panel, day grid), a settings surface that owns `Resource`/`ResourceType` and the per-service requirement, and the room's occupancy legible at a glance. Regression test: a service whose requirement is CLEARED books into a full room, which is the only assertion that proves the requirement is data rather than a constant | RES-*; D-30 | M | A-031, A-032, A-034 | Phase 4 |
| 48 | ✅ A-047 | **The other three doors that strand appointments in silence** (AVAIL-05, and the P-1 pattern for the third time — A-041 built the mechanism and wired ONE caller, saying so in its own left-behind). Verified in `apps/web/lib/settings/availability-actions.ts`: `addAbsence` returns `stranded` and a `conflictsDay` link; **`addWeeklyWindow` returns `{ ok: true, message: 'Hours added.' }`**, `saveDateOverride` returns `'Override saved.'`, and `removeWeeklyWindow`/`removeDateOverride` return a bare `{ ok: true }` — no conflict count, no link, nothing. **The deletes are the worst of the four and are the ones nobody listed:** removing a Thursday window IS "I don't work Thursdays any more", and it silently orphans every Thursday booking on the books; `deleteWeeklyWindow`/`deleteDateOverride` also take no actor, so unlike every other availability write since A-037 the log cannot say who did it. Nothing may refuse (D-2, A-007) — this is the sentence that comes back, exactly as A-041 shaped it. `appointmentsInRange` and the `conflictsDay` link already exist and are the whole implementation. Regression test: deleting a weekly window with future appointments inside it returns their count and leaves every one of them untouched | AVAIL-05; D-2 | S | A-041 | Phase 4 |
| 49 | ✅ A-048 | **The outbox is not ready for a real driver, and that is cheapest to fix BEFORE one lands** (three `ponytail:` ceilings that are one feature). `dispatch.ts` has no claim-before-send, so two overlapping runs both pick up the same `pending` row and send it twice; `reminders.ts:81` is a check-then-act on `dedupeKey` with the same gap; `provider.ts` derives the "queued"/"sent" word **build-wide** rather than per row, so the day a driver lands every message ever queued retroactively reads "sent", and `dispatch.ts` discards the adapter's `externalId` because there is no column to keep it in. All four are invisible today because the wired adapter is a console log — and all four become a client texted twice, and a log that cannot say which driver handled what, on the first day Twilio is real. The claim is `UPDATE ... WHERE status = 'pending' RETURNING` (the pattern A-003 already established here) or a fifth `sending` status; the provenance is one migration adding `externalId`, which the real driver needs anyway to reconcile. **Doing this after the adapter means debugging double-sends against a live SMS bill.** Regression test: two concurrent `dispatchPendingNotifications` calls over one pending row produce exactly one send | NOTIF-01; D-14 | M | A-044 | Phase 4 |

### Rough order, unscoped

Multi-**provider** chains → real Resend/Twilio adapters behind the existing seam → multi-user staff auth + roles → recurring appointments ("every 4 weeks") → tzdata-drift reconciliation job (spec X-5).

> **Superseded in part by the Phase 5 pass below** (written 2026-08-24): recurring appointments became A-049 and multi-user staff auth + roles became A-050, both promoted because they are what a real salon's week is actually made of. The adapters and the tzdata job stay here, with the reasoning stated at the end of that section.

> **Correction to a claim this section used to make.** It read "the first three exist as D-12 schema affordances precisely so none of them is a migration." That is true of the *tables* and false of the *constraint*. `ServiceSegment` and `Resource` cost no migration to populate — A-029 and the resource-modeling half prove it — but the moment either one is allowed to **change what may overlap what**, `appointment_no_overlap` has to be rebuilt, because a single range per row can express neither a hole (segments) nor a second axis (resources). D-12 bought the cheap half of both features, which was the point; it did not buy the expensive half, and the backlog should not have implied it did.


### Phase 5, first pass — scoped at the Phase 4 close-out

> Written 2026-08-24, after A-048 emptied the backlog for the fifth time. **The salon-operator review is still owed and this is not it** — Phase 4's note asked for one before A-048 and it did not happen, and it did not happen here either. So this is an engineering scoping pass over the eight remaining `ponytail:` ceilings, the `**Left behind:**` entries in `PROGRESS.md`, and the two unanswered open questions (OQ-4, OQ-5). **The ordering below is the half that most needs a front-desk opinion — treat it as provisional until one is taken.**
>
> Every claim was re-verified against the code rather than taken from the note that predicted it; where a row says "verified", that means a grep or a schema read done in this session, and the method is stated so the next person can repeat it.
>
> **The organising fact of this phase:** the product can now model a salon's room, refuse honestly, and say what every change stranded — but it still cannot *reach a client*, and it cannot book the appointment a salon's regulars actually make.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 50 | ✅ A-049 | **Standing appointments — "same time every four weeks", which is what a salon's forward book is mostly made of.** Built in two sessions: part 1 (`02463d3`) the rule, the schema and the write path — `AppointmentSeries` storing the rule on the CALENDAR axis (`anchorDay CHAR(10)`, `wallTime CHAR(5)`, `intervalWeeks`), `packages/core/scheduling/series.ts` planning occurrences by pure string arithmetic, `createSeries` booking each one through `bookAppointment` unchanged and PARTIALLY; part 2 the surface — two number fields on `/staff/book`, a summary that lists EVERY week including the ones that did not take and why, and a "2nd of 3, every week" marker on the appointment detail with the rest of the series beside it. D-34 settled the shape (an occurrence detaches; the series is not re-planned) as a recorded DEFAULT rather than an owner answer; **D-35 settles the surface and answers the question this row asked — there is no "cancel the remaining occurrences" button, and the reasoning is written down rather than left as an omission.** | new (needs a story ID) | L | A-014, A-047 | Phase 5 |
| 51 | ✅ A-050 | **Per-person staff credentials, a two-role split, and a brake on both doors.** A-037 gave the salon four NAMES on the audit trail while leaving one shared credential under the desk, and anybody who signed in could open `/staff/dashboard` — revenue, utilization and every stylist's no-show counts. Built: a `StaffRole` enum (`owner`/`staff`) on the rows that already existed, backfilled to `owner` for every row that already had a password; email + password grantable per person from the roster screen; the dashboard and its drill-down behind `requireOwner()`, read from **whoever is at the desk** rather than from the account that signed in; the login limiter (10 per 15 min per email) and the desk-PIN limiter (5 per 5 min per person) that A-005, A-037 and A-044 each left as a note, both counting failures only; and a last-owner guard that refuses to demote OR deactivate the only owner. Off-boarding moved behind the owner role too — deactivating somebody ends their live sessions, so it is a credential being taken away. **D-36** records the shape and what was deliberately not built (per-screen permissions, a manager tier, email password reset — the last waits on the real mail adapter). | D-9, D-33, **D-36** | M | A-044 | Phase 5 |
| 52 | ✅ A-051 | **A failed notification is retried, and what is stuck has a screen.** Verified before building: the A-048 claim matched `pending` or a stale `sending` and nothing else, so a row that reached `failed` was terminal — invisible against a console adapter, and a client who is never told on the first day a real provider has a bad minute. Built: a pure `core/notifications/retry.ts` (the transient/permanent split driven by `ChannelSendError.code`, which had existed unread since A-004, and a backoff table of a minute / five / twenty-five / two hours over five attempts); `nextAttemptAt` as a column so a waiting row is `pending` with a predicate rather than a status of its own; a no-recipient row failed immediately instead of being handed to the adapter as an empty string; and `/staff/messages` — the given-up and the still-trying together, the provider's own reason on each, a count on the landing page, and a "send it again" that resets the attempt budget. **OQ-5 settled both ways as a recorded default (D-37):** no second reminder touch, and the call-down list stays in time order while carrying the ticket value and the no-show flag so the desk can triage without the list being reordered underneath it. | NOTIF-01; D-14; **OQ-5**; **D-37** | M | A-048 | Phase 5 |
| 53 | ✅ A-052 | **"Who blocked Dana's 2–4, and why?" now has an answer, on the availability screen itself.** `createdByActor`/`actorRef` have been written on `WeeklyWindow`, `DateOverride`, `TimeOff` and `AdHocBlock` since A-007; nothing rendered either column. Built: `resolveStaffNames` extracted from A-037's appointment-log resolver into `packages/db/auth` and reused by both readers (one lookup for the whole page rather than per-row), `actorWord` exported from `event-language.ts` so both the event log and this screen render the SAME fallback words for a customer link / the system / a pre-A-037 row, and a "set by"/"blocked by" line on every weekly hour, date override, and absence — never guessed at when the row predates the column. Closes the oldest outstanding operator finding in the file (R-8, first raised at A-007). | AVAIL-01..04; R-8 | S | A-037, A-047 | Phase 5 |
| 54 | ⛔ A-053 | **BLOCKED, not deferred — the operator review at the Phase 5 close (`docs/reviews/12-operator-review-phase-5-close.md`) changed this row's status and the reason is worth keeping.** OQ-4 was recorded as a data-shape question ("sequential soft-hold or parallel first-to-accept?"). It is not one yet: **"a sequential soft-hold takes a Saturday 2pm off the market for 30 minutes on the authority of an offer that was written to a console. That is perishable supply destroyed by a message nobody received."** The gate is not OQ-4, it is a real notification channel. A-023's staff panel plus A-043's "What's opened up" **is** the v1 for a 4-chair salon — that is how one actually works the list — and the operator recommends spending the effort on making the manual call one tap instead. **Do not start until a real adapter has run.** | WAIT-02; **OQ-4** | M | A-043, A-051, a real channel | Phase 5 → blocked |
| 55 | ✅ A-054 | **Demo checkpoint 4 — walked at the seams, and it found three defects, one of them three items old.** Full transcript: `docs/reviews/11-demo-checkpoint-4.md`. **(1)** The dispatcher sent whatever was queued, so an appointment cancelled a minute after its reminder was enqueued produced the cancellation notice AND then a reminder for it — and **A-051's retry turned that millisecond race into a two-and-a-half-hour window**, sending a reminder naming Tuesday for an appointment moved to Wednesday. Fixed by asking whether a reminder is still true immediately before the provider is called; only the reminder can go stale, because it is the only message that makes a claim about the future. **(2)** The reminder revoked the link she was holding at ENQUEUE time, and D-28's own argument for revoking ('the reminder always carries a fresh link') is about DELIVERY — the walk revoked her link, failed the reminder permanently, and left her with a dead link and no replacement. **D-38** narrows D-28 to that one caller using D-28's own reasoning. **(3)** The staff booking panel applied whichever slot response arrived LAST rather than the one asked last, so changing the day while the previous day's times were in flight booked the day the desk had just left — under a heading naming the new one. Fixed with a monotonic request id. Debt folded in: `testing/reset.ts`'s "fail loudly" claim disproved by measurement and corrected (CASCADE sweeps unlisted tables silently; three added); `resolvePrefill` and the public `/book?service=` contract deleted on the owner's answer; `manage.spec.ts`'s hydration race **could not be reproduced** in nine runs and is reported as such rather than papered over with a retry. | — | M | A-048 | Phase 5 |

> **Deliberately NOT next, and why.** The real Resend/Twilio adapters are the obvious candidate and they are the owner's to unblock, not an engineering decision: `logging-adapter.ts` argues — correctly, and the argument still holds — that with no API key, no verified sending domain and no approved SMS campaign, a "real" driver could not be run once, let alone tested, and would ship as an untested HTTP call that merely looks finished. A-048 made the outbox ready for that swap; A-051 makes it survivable. **The day the accounts exist, the adapter is a small row and everything around it is already built.** Multi-provider chains and the tzdata-drift reconciliation job (spec X-5) both stay unscoped: the first is a data-shape question a 4-chair salon has not asked, and the second has never once been observed to bite here.

### Phase 6 — scoped 2026-08-24 from the operator review at the Phase 5 close

> **This ordering is the operator's, not an engineer's, and that is the point.** Full review and verification table: `docs/reviews/12-operator-review-phase-5-close.md`. Three consecutive scoping passes said the ordering needed a front desk's opinion and proceeded without one; the review that finally ran says one of those orderings was wrong, and names the cost — A-049 was an L that took two sessions and went ahead of A-055 and A-056, "each of which costs money every week", on the strength of an engineer's belief about a front desk. **Every factual claim behind these rows was re-verified against the source before it was written down** (the table in the review). Rows are in the operator's priority order; the sizes are mine.
>
> **The verdict that reframes the phase:** the build is *stronger* than expected on the chaos of a Saturday — check-in on the chip, the walk-in door, any date reachable, a sick stylist's clients listable and movable. Where it breaks is **change of mind**: "the one thing a booked appointment cannot do in this system is become a different appointment."

| # | ID | What & why | PRD | Size | Depends | Phase |
|---|---|---|---|---|---|---|
| 56 | ✅ A-055 | **A booked appointment cannot change what it is FOR, and that is the biggest hole in the product.** Verified: nothing writes `AppointmentServiceLine` outside the booking path — one `findMany` in `reschedule-actions.ts` and a test fixture, no writer. So "she's in the chair and wants her roots done too" has three answers and all three are wrong: cancel-and-rebook writes `cancelled_late` on an innocent client and fires a cancellation notice at her *while she is sitting in the chair*; a second adjacent appointment is refused the moment the cut's `bufferAfter` meets the colour's `bufferBefore` (the case D-23 spells out and forbids); an override trains the desk to dismiss the marker D-8 rests on. The reverse costs more — she books a full head, wants a root touch-up, and 90 minutes of Dana's Saturday stays unsellable because nothing can shorten a visit. Scope: edit the service lines on the appointment detail (add/remove/reorder) through `rescheduleAppointment`'s existing same-row-UPDATE-in-one-transaction shape (D-6), recomposing the body exactly as VISIT-01 does at booking, rewriting blocks and the resource hold, re-running the engine inside the transaction so a lengthened visit that collides is refused with the same `SlotTaken`/`NoResourceFree` vocabulary and BOOK-05's override. New lines snapshot current price/duration (D-18); existing lines keep theirs — **never re-price a line already agreed**. Qualification is A-019's SVC-02 rule reused. One event naming what changed. **Zero `cancelled_late` anywhere in the path — that is the regression test.** Shortening frees the tail into `/staff/opened` for free, because it derives | VISIT-01; D-6, D-18, D-23 | M–L | A-028, A-033 | Phase 6 |
| 57 | ✅ A-056 | **"Anything Thursday? I don't mind who" — SVC-02 was specified and never built.** Verified by grep: the only hits for the rule are the waitlist's *preference* field, a tiebreak comment in `providers.ts`, and a UI label. Neither booking path implements it. `/staff/book` will not ask for times without a `?provider=`, so one day is four passes and "Thursday or Friday" is sixteen — the desk stops doing it and says "let me ring you back", which is a booking lost. The public flow makes "Who would you like to see?" a **mandatory** step with no *no preference* option, so a first-time client who has never heard of Dana or Priya picks the top name or leaves — **which is why the senior is solid and the junior is at 40%, the exact utilization gap A-024's dashboard reports without being able to explain.** `walkInOptions` already does the cross-provider search but returns one time per provider anchored to `now`, so it cannot answer a question about Thursday. Scope: an "anyone" mode on the staff panel and "No preference" on the public flow, both merging every qualified provider's open times into one time-ordered list with the name on each row; assignment on submit follows SVC-02 as written (fewest booked minutes that business date, ties by `displayOrder` — deterministic, therefore testable); per-provider duration overrides respected. Acceptance in desk terms: **from a cold start, answer "anything Thursday afternoon, anyone" on one screen and book it** | SVC-02; BOOK-01, BOOK-04 | M | A-010, A-017 | Phase 6 |
| 58 | ✅ A-057 | **"End this series here" — D-35 was wrong and D-39 overturns it.** The structural argument, which is the whole finding: **creating six appointments is one action and undoing them is six, and any product where the undo costs six times the create teaches the desk not to use the create.** Mrs Kerr rings on a Saturday with six four-weekly Tuesdays booked; D-35 sent the desk through six detail pages with six cutoff judgements, six reason boxes and six messages, so they do two, mean to come back to it, and Dana's next four 2pm Tuesdays are held for someone who is never coming — then land as four no-shows on a client's record that were the salon's fault. D-35's own objection ("which ones did you mean") has an answer: **future from this one**, because past occurrences already happened and cancelled ones are already cancelled. Its cutoff-per-occurrence and message-per-client objections are arguments for a **preview**, not against the action. Scope: "End this series here" on any occurrence; a preview listing each future occurrence with its date and whether it falls inside the cutoff (`service.cutoff ?? business.cutoff`), same shape as `previewPush`; partial and self-describing per D-26/A-019; one reason typed once, one event and one outbox row per occurrence, D-32's suppress-if-I-already-rang tick on the whole action; the series row survives (`seriesId` stays as provenance, D-34). Also answers "move my standing 2pm to 2:30 from now on" — end here, rebook from here, **no rule editor** | **D-39**; D-26, D-32, D-34 | S–M | A-049 | Phase 6 |
| 59 | ✅ A-058 | **The public flow books one service, and books things that should never be booked online.** Verified: `booking-flow.tsx` holds a single `service` and posts a single `serviceId`, while D-23's own text says half the Saturday book is cut+colour — so she books "Colour" alone at two hours, arrives wanting a cut too, and 45 minutes have to come from somewhere in a booked column. The same defect's other half: **`Service` has no "bookable online" flag** (verified, zero hits in `schema.prisma`), so a first-time client can self-book a colour correction or a full-head bleach with no consultation and no patch test. Scope: the public flow accepts an ordered set of services and posts the array `bookAppointment` already takes, showing the composed visit's duration and price rather than per line; `Service.bookableOnline` (default true) filtered out of the public catalogue **and** out of `daysWithAvailability`, still fully bookable by staff; a service that is staff-only says what to do ("call us — this one needs a consultation first") rather than silently vanishing. Stays inside BOOK-01's ≤5 screens — multi-select on the existing services screen, not a new step | BOOK-01; D-23 | M | A-010 | Phase 6 |
| 60 | ✅ A-059 | **Running late tells nobody, so the desk keeps the list on a Post-it — which is the shadow calendar.** `setRunningLate` upserts a row and notifies no one: the delta is stored, the grid is amber, the engine has stopped selling 11:15, and every one of Dana's afternoon clients is still on their way at the time on her confirmation. The honest alternative — push the column — is exactly what D-26 says will refuse or half-move on a packed day. Scope: when a delta is set, show the affected appointments for the next two to three hours (client, scheduled time, **projected** start with the delta applied, `tel:` link, and the flag/note already on the chip), plus a **"told her" mark per row** — stored, actor-stamped, cleared when the delta clears — so the second person at the desk does not re-ring the first six. **No message is sent by this action**: the channel is a console log and pretending otherwise is worse than silence, so it keeps A-044's *queued* wording. Fold in: the push-by field accepts a **negative** delta (verified — `push-column.ts:402` only refuses zero) and nothing says so; "she's caught up, pull it back 20" is a real instruction and today it is a hidden feature | D-22; APPT-04 | S | A-018 | Phase 6 |
| 61 | ✅ A-060 | **The desk decides `cancelled` vs `cancelled_late`, under pressure, with two buttons side by side — and the owner makes staffing decisions on the result.** `STATUS_ACTION_LABELS` offers "Cancel" and "Cancel (late)" with no default and no guidance, and the transition table allows staff either at any time, so the number on A-024's tile and the `lateCancels` column on every client surface is **an artifact of which button was nearer the thumb**. The system already knows the answer: it has the cutoff and the clock. Scope: staff cancel becomes ONE button, deriving the status from `service.cutoff ?? business.cutoff` against `now` exactly as the customer path already does; one explicit escape beside it ("she gave us proper notice" / "this one's on us") which downgrades to `cancelled`, requires a reason, and records the overruled classification in the event; **a cancellation caused by the salon must never land as `cancelled_late` on an innocent client's rolling count** (`reliability.ts` counts by status only and cannot tell the difference today); one drill-down for "how many did we overrule, and who" | APPT-06; D-19 | S | A-012, A-020 | Phase 6 |
| 62 | ✅ A-061 | **The call-down list forgets who has already been rung.** Eighteen unconfirmed for tomorrow, the desk gets through nine, three no-answers, the phone rings and a walk-in arrives — and at 4pm the list looks exactly as it did at 2pm, so somebody starts at the top and rings six people twice. Scope: per-row "tried — no answer" / "left a message", actor- and timestamp-stamped, visible on the row and surviving a reload; a tried row greys or sinks but **the list stays in time order** (D-37(b) is right and this does not disturb it); cleared when the appointment confirms or the day rolls. **This one must be STORED, not derived** — "we tried" is not derivable from anything, and it is the exception that proves operator R-7's rule rather than breaking it. A double call reads as chaos to the client and is exactly why desks keep the paper list beside the screen | APPT-02; D-37(b) | S | A-021 | Phase 6 |
| 63 | ✅ A-062 | **A printable day sheet, because the terminal does not come to the backwash.** Every salon the reviewer has run prints the day at 8:45 and pins it at each station; the moment the desk prints a screenshot or writes the day out by hand, the paper book is back and it starts collecting the walk-ins. And when the broadband goes down mid-Saturday the printed sheet is the difference between a normal day and closing early. Scope: `@media print` on `/staff/day` and `?provider=` — one column per page, time, client, phone, services, duration, pinned note and flag, and blank space for the stylist's own scribbles. Include the running-late delta and **today's date, so a sheet in a bin cannot be mistaken for today's**. No new route, no PDF library. *Reviewer's own confidence: medium-high — "a habit from my own two businesses, not something I can prove from your code"* | — | S | A-016 | Phase 6 |
| 64 | ✅ A-063 | **PROVE IT FIRST: one client's two appointments may hold two chairs.** The supported way to do "cut with Dana, then colour with Priya" is two appointments, because two providers never collide on the provider axis — but `AppointmentResourceHold` spans the whole envelope including buffers (RES-02), so the cut's after-buffer and the colour's before-buffer overlap and **the same client holds two of four chairs for 20–25 minutes**, during which the room reports full and refuses a real client on the authority of a chair with nobody in it. No client-axis check could notice, deliberately — D-17 rules one out to protect the mother-and-daughter case, and that decision is right; this is an unnoticed consequence of it. **The first job is the fixture that proves it is reachable against the seeded Saturday with the real buffers** — the same rule A-034 set for itself — and if the seed's buffers do not produce an overlap, say so and close the row. If reachable: when two appointments share a client and their envelopes overlap, reuse the same resource rather than allocating a second — the chair follows the client, not the provider. **Do not build multi-provider chains to fix this**; two appointments is the right model and only the chair accounting is wrong. *Reviewer's own confidence: medium — derived from the hold's span and `findFreeResource`'s predicate, not observed running* | RES-02; D-17, D-30 | S | A-034, A-046 | Phase 6 |
| 65 | ✅ A-064 | **The product has no front door.** `/` was the scaffold's "Bookable" placeholder, so every route into this system was one somebody already had to know: `/book`, `/staff`, a manage token in an email. A salon's website is how a client finds the salon at all, and the failure mode of building it as a brochure is that it becomes a **second source of truth** — a typed price list that lies the first Monday after a price rise, a stylist listed for colour she is not qualified for (SVC-02 then refuses the visit she was told to book), an hours table that still says Tuesday after the salon changed to Wednesday. Scope: a `(site)` route group — home, services, stylists, visit — where **every figure is read from the rows the desk works from**: the catalogue (A-006), the roster and its qualifications (A-025/A-019), the weekly windows and date overrides the slot engine itself reads (AVAIL-02), the chair count (A-031). A-058's `bookableOnline` service appears on the price list and wears "Call us" rather than a Book button. Contact details become nullable columns on `Business` and render only when set. `force-dynamic`, same reasoning as `/book`. The brand chrome lives in the route group and **not** the root layout: `/book` and `/manage/[token]` are public but transactional, and a header naming the salon above a flow naming the salon gives every text locator two matches — A-062's defect, avoided rather than repeated | — | M | A-006, A-025, A-031, A-058 | Phase 6 |
| 66 | ✅ A-065 | **Demo checkpoint 5 — walk the Phase 6 boundary before scoping Phase 7.** Checkpoints 1–4 each found a defect sitting inside an item already marked ✅ and invisible from within the item that introduced it; the rule is to walk one when a milestone closes, not when convenient. Scenes at the SEAMS between Phase 6 items rather than down the middle of any one. Found three, all the same blind spot: **A-063 split the chair invariant in two, threaded every write path, and left three readers still asking the old question** — the availability sweep (`fullSpans` counted holds, so a room with an empty chair stopped OFFERING it, which is A-063's own stated harm surviving on the one surface a client meets), A-018's in-memory push planner (recorded by A-063 as "a seating cosmetic — nothing refuses"; it refuses, and `canPush=false` on a running-late Saturday), and — separately — **A-057 classifying `cancelled`/`cancelled_late` from the clock alone with no escape**, so the salon ending a standing booking, or MOVING one via "end here, rebook from here", stamps a late cancellation on an innocent client's record for twelve months. Walked clean: the client merge against the shared chair (the hold-writing trigger resyncs `holderKey`). Left behind: the review doc's one-sentence lesson, now a CLAUDE.md rule — a constraint is never one edit either | — | M | A-057, A-060, A-063 | Phase 6 close |

### Phase 7 — scoped 2026-09-01 from the operator review at the Phase 6 close

> `docs/reviews/14-operator-review-phase-6-close.md`. **One shape, found repeatedly: Phase 6 added three new ways for the state of the day to change and did not tell the readers that model the day.** That is checkpoint 5's lesson a layer up, and it is now a CLAUDE.md rule — *a state change is never one edit*. Rows 1–2 are that defect twice; rows 3, 4 and 6 are doors nobody closed; 5 is data already loaded and thrown away. Every claim was verified by grep before the row was written, twice and independently, and the reviewer's own confidence is recorded where it is not high.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 67 | ✅ A-066 | **Pushing the column leaves the running-late delta standing, and every screen then double-counts it.** Dana is 40 behind, the desk sets +40 (correct — the site stops selling her 11:15), she does not catch up, so at 12:30 they push the column +40. Every appointment moves and **the delta is still 40**: Mrs Hall's 15:10 chip reads "→ likely 15:50", the ring-round says phone six clients about a delay already applied to their booked times, D-41's "told her about 40 min" marks still read as handled, and the engine keeps subtracting a 40-minute `running-late` interval from a column that is now honest — refusing to sell a gap that genuinely exists. Verified: zero hits for `runningLate` in `push-column.ts`; `clearRunningLate` has exactly two callers (`setRunningLate` at ≤0, and the Clear button); `view-model.ts:329` projects `shift(startAt, runningLateMinutes)` over a `startAt` that `push-column.ts:541` has already moved; `day-view.ts:317` feeds the same unreduced minutes to `lateCallList`. Scope: a confirmed push of N reduces the delta by N (floor zero, clearing the row at zero) **in the same transaction as the moves** — a second write path that "usually" runs is how this comes back; the preview states the resulting delta before the desk commits ("moves 6, leaves 2, Dana then shows 0 behind"); `RunningLateTold` marks whose `minutesToldAbout` no longer matches go stale by A-059's existing rule, never deleted — the desk did make those calls. **The partial-push arm needs a decision written down BEFORE the code (D-43, amending D-41): reduce by the amount actually applied, or refuse to reduce and say so — silently reducing on a half-move is worse than not reducing.** Same for whether A-059's negative pull increases the delta or is explicitly out of scope. Regression test: set +40, push +40, assert every chip's projected time equals its `startAt` and `lateCallList` is empty. *"This is the failure that puts the paper book back on the counter"* — and its second-order cost is worse: after it, the delta stops being set at all. *Reviewer's confidence: high on the defect, medium on the partial arm* | APPT-04; **D-41 → D-43**; D-22, D-26 | S | A-018, A-059 | Phase 7 |
| 68 | ✅ A-067 | **Time freed by anything other than a cancellation never reaches "What's opened up".** Mrs Hall is booked cut + full head, two hours on a Saturday; she sits down and wants the roots only. A-055 does exactly what it should — the visit shortens, no cancellation, no notice — and **90 minutes of a Saturday afternoon becomes invisible**, so the waitlist entry two screens away that fits it is never matched. Same for a reschedule off the day and a cross-provider reassign. Verified: `opened.ts:84` filters `SLOT_FREEING_STATUSES` (cancelled/cancelled_late only) and a shortened visit is still `booked` — while `visit-actions.ts:65` calls `revalidatePath('/staff/opened')` after a service change, revalidating a page that structurally cannot show it. **A-055's own row claims "shortening frees the tail into `/staff/opened` for free, because it derives". That claim is false and this row is its correction.** Scope: report every span that recently stopped being occupied and is still empty — the tail of a shortened visit, the vacated range of a reschedule, the vacated range of a reassign; **stay derived**, using the event log's `services_changed`/`rescheduled`/`provider_changed` (which record both sides) as the recency bound the `updatedAt` heuristic is standing in for; each row says what freed it in the desk's words ("Mrs Hall dropped her colour", "moved to Thursday") because the follow-up call differs; provider, span, minutes and the same tel: link the cancellation rows carry; keep all three existing bounds (future, recent, still empty). Test: shorten a two-hour visit to thirty minutes, assert the 90 minutes appears, book over it, assert it disappears with no clearing code. *Operator's own numbers: $150–180 a slot, two to four a week, ~$300–500 a week of supply nobody can see* | WAIT-02; D-6, D-31 | S–M | A-043, A-055 | Phase 7 |
| 69 | ✅ A-068 | **An appointment's client cannot be attached or corrected after it is booked, and the workaround now brands the client.** Two weekly cases. (a) A walk-in is typed in as nothing but a time — BOOK-04, and right, you do not take a phone number while she is standing there — then rebooks at the till, and **her visit is orphaned forever**: on no client record, counting toward no reliability, no reminder, and if she returns with her daughter A-063's `holderKey` treats them as two strangers. (b) The desk picks the wrong Sarah Jones of the two D-17 guarantees will exist, discovered at check-in; the only correction is cancel-and-rebook, and **since A-060 that cancel derives `cancelled_late`** — a late cancellation on an innocent client's twelve-month count for the desk's own typo, which is precisely the harm A-055 and A-060 exist to prevent arriving through the one door nobody closed. Verified: the only writer of `Appointment.clientId` after creation is the client merge (`clients.ts:212`); no attach path, no server action — and `schema.prisma:653` already promises one ("NULLABLE — BOOK-04 requires booking with no client record, identity attached later"). Scope: "Who was this?" on the appointment detail using the SAME search-or-create control `/staff/book` uses (one client picker in the product, not two); attach, change and **detach** are one action with different words, each writing an event naming both sides; allowed from every non-terminal status **and from `completed`/`no_show`** — "she was a no-show and it turns out she is Mrs Kerr" is a real correction and the whole reason to want the record; attaching a past no-show does move her count, and that is correct. **Must be an ordinary row `UPDATE`** so `appointment_write_resource_hold` re-derives `holderKey` (checkpoint 5) — with the mirror of A-063's test: attaching a client to a walk-in whose envelope overlaps her own other appointment collapses them onto one chair | BOOK-04, CLIENT-01; D-17 | S–M | A-035, A-060, A-063 | Phase 7 |
| 70 | ✅ A-069 | **DECIDE FIRST (OQ-17 → a D-number), THEN BUILD: a no-show's time is dead supply and no screen offers it back.** 10:00 colour, ninety minutes; at 10:20 the desk marks no-show and that time stays blocked. A walk-in at 10:25 can only be booked into it through a BOOK-05 override with a typed reason — **a false override marker on a slot that is genuinely empty, which trains the desk to dismiss the marker D-8 rests on.** It is not on `/staff/opened` either. Verified: `status.ts:38` makes `SLOT_FREEING_STATUSES` cancelled-only, `no_show` is in `ACTIVE_STATUSES` and therefore in the constraint predicate and the busy set, and `opened.ts:83` excludes it with a comment saying so. **This does NOT re-open D-7 and must not: `no_show` occupying its time is right for the record, utilization and reliability, and half the product reads that. What is missing is a separate ACTION, not an enum change.** OQ-17, to be answered as a D-number before a line is written: (a) a released-range marker on the row — the mirror of D-8's zero-width override plus `overriddenFromRange`, which the constraint, the busy set and D-16's COALESCE already know how to read — or (b) an explicit `releasedAt` cut so the appointment occupies from `startAt` to the release moment. *"Picking it mid-item is how the last three constraint edits went wrong."* Scope once decided: the action is offered at the moment the desk marks the no-show, not a screen away; it changes no status, no recorded booked duration and invents no override; the released span appears on A-067's list so the waitlist and the walk-in door both see it; utilization and the client's no-show count are untouched, with a test asserting both. **Never automatic** — releasing at N minutes past resells a slot for a client stuck in traffic eight minutes away. *Reviewer's confidence: high on the gap, medium on the shape — may be an L if `AppointmentBlock`'s trigger has to change* | APPT-03, BOOK-05; **OQ-17**; D-7, D-8, D-16 | M | A-067, A-020 | Phase 7 |
| 71 | ✅ A-070 | **The per-visit note is written on one screen and read on no other.** "Patch test done 12/4." "6.3 + 20 vol, 35 min." "Bring the reference photo." The desk types these into the appointment's own note field and **the stylist at the backwash never sees them** — the printed day sheet and the day chip carry only the CLIENT's pinned note. A-062's blank scribble column is then the salon writing the colour formula on paper and binning it at 6pm, and next time you are guessing; the patch-test line is a safety surface kept where nobody looks. Verified, and it is an oversight rather than a decision: `day-view.ts:295` already selects and returns `notes` alongside `clientNotes`, and `view-model.ts:307` maps only `clientNotes → pinnedNote`, **dropping `notes` on the floor**. Scope: the view model carries it; the sheet prints it on the row **visually distinct from the pinned note** (one is about her, one is about today); the chip shows it truncated with it in the accessible label, exactly as `clientNotes` already is at `view-model.ts:342`; and it must be enterable from the day grid or the check-in path, not only the detail page — *"if it takes three taps to write '6.3 + 20vol' it goes on the scribble column instead, which is the failure this closes"*. The scribble column stays (it is where the walk-in and "back at 3" go). **The two notes are never merged** — `Appointment.notes` exists precisely because per-visit notes bury the allergy line | CLIENT-03 | S | A-062, A-016, A-035 | Phase 7 |
| 72 | ✅ A-071 | **"Anyone at two" loses the race and offers an override instead of the next free stylist.** The client had no preference; the row said 14:00, Dana, 3 free. The desk takes a phone call, comes back, submits — and the public flow has just taken Dana. The panel says "That time is not free" and offers **an override that would knowingly double-book Dana**, while Priya and Tess are both free at two o'clock. So the desk either takes the override (wrong) or starts the search again with the client on the phone, and the whole premise of A-056 — that at two o'clock the stylists are interchangeable — is thrown away at the last step. Verified: `staff-actions.ts:262-281` returns `canOverride: true` with `refusedReasons: ['overlaps-booking']` for `SlotTaken` on every path; nothing distinguishes the anyone path and nothing re-asks `anyProviderTimes`. Checkpoint 5 did not walk this seam. Scope: when the booking came from an "anyone" row and loses the race, re-run `anyProviderTimes` for that instant and return "Dana has just gone — Priya can do it at two. Book that?" as a one-tap confirm rather than a re-search; the ordinary refusal-plus-override appears only when no qualified stylist is free at that instant; **never silently re-assign** — A-056's rule is that what you see is what you book, so the fall-through names a person on a button the desk presses. Same on the public "No preference" path (`public-actions.ts:357`), which should re-offer rather than dead-end a first-time client. Test: hold Dana's 14:00 in a concurrent transaction, submit the anyone row naming Dana, assert the response names Priya and does NOT offer an override. *The override-suggested-over-an-empty-chair case is the same class as A-069* | BOOK-01, BOOK-05; SVC-02; D-2, D-8 | S | A-056 | Phase 7 |
| 73 | ✅ A-072 | **Ringing round a freed slot has no memory of who has already been offered it.** Thursday's three-hour colour cancels on Saturday morning and lands on `/staff/opened` with two waitlist matches and a tel: link — good. The desk rings Mrs Patel, who says "let me check with work". Then a walk-in arrives and the phone goes, and at 4pm the second person at the desk opens the same list, sees the same slot and the same two names, and rings Mrs Patel again — **or promises it to the second name while the first is still deciding**. A-061 fixed exactly this for the call-down list; the list with the money on it never got it. Verified: `CallDownAttempt` is keyed `(appointmentId, forDay)` and read only by the call-down; `/staff/opened` and `/staff/waitlist` have the one-tap tel: link and no attempt tracking. Scope: per-offer marks on the freed-slot row — who was rung, by whom, when, and the outcome ("no answer", "left a message", "she's thinking about it", "she took it"). **This is a RECORD, not a hold**, and that is what makes it buildable while A-053 is correctly blocked: the slot stays sellable to anybody throughout, it is a note about a phone call a human made exactly like `RunningLateTold` and `CallDownAttempt`, **it sends nothing and must appear nowhere near `deliveryWord()`** (D-41's reasoning). Reuse A-061's shape rather than inventing a third — toggle not one-way tick, unique key so a second call re-stamps, actor-stamped. Cleared by the slot ceasing to be free, which needs no clearing code because the list is derived. A test asserting `notificationOutbox.count()` does not move, as A-059 and A-061 have. *Reviewer's confidence: medium-high — high that it is absent, medium that it earns its size at four chairs before a real channel exists* | WAIT-02; D-37(b), D-41; **explicitly NOT OQ-4** | S–M | A-067, A-043, A-061 | Phase 7 |
| 74 | ✅ A-073 | **Nothing lists the clients who have stopped coming.** Tuesday is at 45% and the owner has no list to ring: three hundred clients, eighty of them on a six-week cycle who have not been in for fourteen weeks, and the only way to find them today is to read the client list one record at a time. A-040 fixed the other half of this (rebooking at checkout); this is the largest untapped lever left. Verified: `packages/db/reports/` holds `dashboard.ts` and `overruled.ts` and nothing else — `clientHistory` is per client and there is no cross-client recency query. Scope: one list of clients whose last `completed` visit is older than N weeks with nothing booked, longest-lapsed or most-valuable first, carrying last provider, last service, last spend and the tel: link; **reuse A-072's attempt marks** so it remembers who has been rung — a lapsed list without that is a Post-it within a week; N is a number on the report, not a setting nobody will tune; owner-only like every report surface (D-36); exclude anyone flagged, because a no-show-blocked client is not who you ring to fill a Tuesday. *Operator's own numbers: thirty calls books six to eight, $600–900 in an otherwise quiet week — "my number from my salon, not an industry figure".* **Reviewer's confidence: medium — high that it is valuable in a real salon, medium that it belongs in Phase 7 of a learning build: "it is a report, not a correctness problem, and the seven above are all about the book being wrong"** | RPT-01, CLIENT-02 | M | A-024, A-072 | Phase 7 |

> **What the operator said NOT to build at the Phase 6 close, which is as useful as the list above.** **Do not make `no_show` free the slot** — A-069 is a release ACTION, not an enum change; a no-show that stops occupying its time shows a gap where the salon genuinely lost ninety minutes and corrupts utilization, and D-7 plus the CLAUDE.md invariant are right. **Do not fix the room strip's overlapping blocks** — after A-063 one client's two holds render as two stacked bars in `room-strip.tsx`, it reads as a double-booked chair for about two seconds, and "a lane-splitting renderer is the first step toward the floor plan the last review told you not to build"; one label ("shared — Nadia Okafor") if it ever irritates anyone. **Do not rebuild `/staff` as a dashboard** — it is a thirteen-link index tapped once a morning and the desk lives in the day grid. **No Print button and no free-gap rows on the day sheet** — A-062's reasoning stands, and "45 min free" twice a page costs rows a stylist has to scan under pressure. **Do not auto-derive the running-late delta from `startedAt`** — D-22 refused it and was right; A-066 is about a push settling a delta somebody claimed, not about the system guessing. **Everything on the Phase 6 do-not-build list stands unchanged.**
>
> **A-053 stays blocked, and the Resend/Twilio accounts are still the owner's most valuable non-engineering action — six of these eight rows were deliberately scoped so they do not need it.**


> **What the operator said NOT to build, which is as useful as the list above.** A-053's automated waitlist offers are **blocked on a real channel, not deferred** (row 54, restated). No second reminder touch (D-37(a)). No re-ranking of the call-down (D-37(b)). No week or month grid — unchanged from the last two reviews, the date box solved it. No chair-visualisation screen — "A-046's room strip is already at the edge of useful; do not grow it into a floor plan." No third staff role, per-screen permissions or manager tier — "D-36 got this right and the resistance to a third role is the valuable part of it." No series rule editor — A-057 plus a rebook covers it, and a rule editable underneath booked occurrences is a class of "which ones moved" bugs for a case that happens twice a year. No multi-provider chains as an epic — A-063 fixes the one thing actually broken about two appointments, for a fraction of the cost.
>
> **And the owner's most valuable action is not an engineering row.** The real Resend/Twilio adapters: `logging-adapter.ts`'s argument for not building them still holds, but the accounts are now the gate on A-053, on ever evaluating D-37(a) honestly, and on the truthfulness of every "was she told?" column in the product. That is worth saying out loud rather than leaving at the bottom of an unscoped list.

### Phase 8 — scoped 2026-09-02 from the operator review at the Phase 7 close

> `docs/reviews/15-operator-review-phase-7-close.md`. **One shape again, one door further along: A-069 added a fourth way for an appointment's occupancy to change — a CUT — and told the two range triggers, but not the third thing that models occupancy (the chair's BODY), not the write path that moves a column, and not the path back.** That is both CLAUDE.md rules firing at once, on one column. Rows 75–76 are that defect, and **both were proved by running code against the local database, not by reading it** — the SQLSTATEs are in the review. Row 77 is the largest standing hole in the product and needs an owner decision first. Row 78 is A-073's own left-behind. The process note the reviewer left is now a CLAUDE.md rule: *when a new column changes what a range means, the readers to grep for are the ones that keep their own copy of the same fact under a different name.*

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 75 | ✅ A-074 | **A released no-show still holds its CHAIR, and the room says the chair is free — so the desk is offered the slot and then refused it.** 10:00 colour, no-show at 10:20, time released; the day grid paints a bookable "45 min free" chip over the tail because gaps derive from the busy set, and the walk-in at 10:30 gets `NoResourceFree: Every Chair is taken for that time` on a chair with nobody in it. **This is A-063's stated harm word for word and checkpoint 5's third finding, arriving through the door A-069 opened.** Verified by RUNNING it: `20260901220000_release_no_show/migration.sql` rewrites `appointment_write_blocked_range` and `appointment_write_blocks` and does not touch `appointment_write_resource_hold`, which still writes `bodyStart`/`bodyEnd` from `NEW."startAt"`/`NEW."endAt"` (`20260831180000_shared_chair`, §4) — and `appointment_resource_body_no_overlap` is unconditional on the holder. `resources.ts:105-107` asks the constraint's question; `resource-load.ts:145-149` asks a different one (`findRoomFullIntervals` filters on the blocked range only), so the availability sweep sees the cut and the chooser does not. **A-069's own header comment claims the chair holds follow for free; the envelope does and the body does not, and A-069's PROGRESS entry repeats the claim — this row is its correction.** Scope: `appointment_write_resource_hold` truncates `bodyEnd` to `releasedAt` under the same status guard the blocked-range trigger uses; a test that seats the walk-in in a **ONE-CHAIR** room, which is the only fixture that can fail — `release-time.test.ts` creates no `ResourceType`, no `Resource` and no `requiredResourceTypeId`, so the whole item shipped with the room untested; the mirror assertion that the time she DID hold still refuses a body, so the fix cannot overshoot; and grep everything else that models the room from `bodyStart`/`bodyEnd` (`chairAlreadyHeldBy` at `resources.ts:157`, `push-column.ts:378-397`) rather than assuming. **Does NOT re-open D-7 or D-44** — it is the body range following a cut the parent row already made. *One refused walk-in is $45–65; the cost that matters is the desk learning to reach for the BOOK-05 override on empty time, which is the training A-069 exists to prevent* | RES-02, BOOK-05; D-44, D-8 | S | A-069, A-063 | Phase 8 |
| 76 | ✅ A-075 | **Pushing a column over a released no-show is a raw 500, and there is no way back from a release.** Two failures, one column, both because `releasedAt` is a value the paths that MOVE an appointment have never heard of. **(a)** Dana is behind; the desk pushes her column from 10:00. `push-column.ts:113` selects everything in `ACTIVE_STATUSES` from `fromAt` — `no_show` is in that list — so the released colour is in the move set, `startAt` goes to 10:30 while `releasedAt` stays 10:20, and the CHECK `appointment_released_within_visit` fires. Verified by running it: preview returns `{"canPush":true}` and the push throws SQLSTATE **23514** into the running-late workflow, which is **the identical defect A-034 was written to close, on the identical function, sixteen items later**; `push-column.ts:665-672` maps `isSlotTakenError` and rethrows the rest, and the whole transaction rolls back so nothing moves on the busiest column of the week. **(b)** She walks in at 10:35, the desk books her into her own released tail, and the `no_show → completed` correction is then permanently refused by the exclusion constraint — **she keeps a no-show she did not earn**, which is the harm A-055, A-060 and A-068 were each built to prevent arriving through a fourth door. And the desk finds out as a crash: `actions.ts:58-67` maps `AppointmentMovedFirst` and `TransitionRefused` then rethrows, and `transition.ts` maps no `23P01` at all. A-069's own test asserts the refusal is NOT a `TransitionRefused` and nobody asked what the desk sees. Scope: the push **excludes terminal statuses from the move set** (a client who did not come cannot run late, and moving a `completed` row's `startAt` rewrites history) — smaller and truer than carrying `releasedAt` along; `previewPush` asks the question the push asks (A-018's own rule) so the preview cannot promise a move that fails; `changeStatus` maps a slot-taken refusal to words. **DECIDE FIRST (OQ-19 → a D-number): what happens when she walks in after a release** — (a) irreversible and said in words, or (b) a guarded un-release while the freed tail is still empty | APPT-04, APPT-06; **OQ-19**; D-44, D-26 | S–M | A-069, A-018 | Phase 8 |
| 77 | ✅ A-076 | **DECIDE FIRST (OQ-18 → a D-number), THEN BUILD: nothing closes the day, and there is no path from `booked` to `completed`.** Six o'clock Saturday, twenty-nine appointments went through, and eleven are still on `booked` or `checked_in` forever because at the till you are taking money, rebooking her and answering the phone. **Nothing anywhere mentions them again**, and three readers are wrong as a result: `dashboard.ts:89` counts minutes for `completed`/`no_show` only, so utilization is understated every week and the owner staffs Tuesday on it; **A-073's brand-new lapsed list rings the wrong people on its first use** (`lapsed.ts:86` takes the most recent `completed` visit and `lapsed.ts:102-107` only excludes her for something booked AHEAD, so a regular seen three weeks ago on an unclosed ticket reads as lapsed — and a client whose visits were never closed is invisible to it entirely); and `reliability.ts:68` counts by status, so a no-show nobody tapped never fires CLIENT-04's block. **It cannot be fixed at the desk today**: `transitions.ts` has no `booked → completed` edge at all, so closing Saturday on Monday is twenty-two taps and writes a Monday check-in timestamp onto a client who sat down Saturday, corrupting APPT-03 to satisfy a table. Nothing lists what is open — `grep -rni "overdue|unfinished|still booked"` returns nothing. Scope once decided: one surface **on the desk's path** with a count on it, exactly as "Opened up (N)" is — never an owner report, it is a six-o'clock desk errand and a management screen will not get done; one tap per row for *she came* / *she didn't*; `booked → completed` and `confirmed → completed` staff edges with `startedAt`/`endedAt` left **NULL** rather than faked, because a missing timestamp is honest and a Monday-morning one is a lie in the audit trail; an ordinary `AppointmentEvent` on every one; bulk-close per day, partial and self-describing (D-26, D-39). **OQ-18, answered as a D-number first: what does an un-closed past appointment MEAN to a report** — (a) nothing, and this row is only the screen that makes the desk close them; (b) the reports derive it, treating a past `booked` as attended, never written; or (c) a job auto-completes N hours after `endAt`. **(c) is on the do-not-build list and the reviewer argues hard against it**: a timer that silently turns a genuine no-show into a completed visit destroys the one counter the product exists to keep. *Operator's own numbers: the $600–900 lapsed round is worth nothing if the first ten names were in last month, and utilization understated by a fifth is the difference between keeping Tuesday's junior on and cutting her hours.* **Reviewer's confidence: high on the hole, medium on the frequency — a fifth to a third of a busy Saturday is operating experience, not something the code can prove** | APPT-01, APPT-03, RPT-01, CLIENT-02; **OQ-18**; D-7, D-26, D-39 | M | A-073, A-035, A-012 | Phase 8 |
| 78 | ✅ A-077 | **The lapsed list's call marks never expire and never say when.** A-073 reused A-072's marks, which is right — but the two subjects have completely different lifetimes and only one was designed for. A freed slot dies on Thursday at 2, so a mark against it is days old at most; **the `lapsed` subject is one row per client, forever**, and the lapsed round is a quarterly errand. So in October the owner reads "left a message — Priya" beside a name, from a call Priya made in June, and skips her — A-061's original defect (a list that lies about what has been done) inverted: not a missing memory, a memory with no expiry. Verified: `lapsed/page.tsx:139-144` renders `OFFER_WORDS[mark.outcome]` and the caller's name and nothing else, while `call-marks.ts:110` has been returning `calledAt` all along. Scope: the date on the row; a mark older than the report's own `weeks` window reads as **stale rather than handled**, copying A-059's existing rule (`running-late.ts:226`) rather than inventing a second one; nothing stored and nothing cleared, because staleness derives from `calledAt` against the window. **Fold in the two other load-bearing left-behinds while the files are open:** A-072's `ATTEMPT_WORDS` still exported from a `'use client'` module and read by a server component (the identical shape that produced a blank 500 on `/staff/opened` — "it demonstrably works" is true and is not a reason), and A-073's flagged exclusion asking its own twelve-month question instead of calling `clientReliability`, which is a second copy of CLIENT-04's window living outside the module that owns it | RPT-01, CLIENT-02, CLIENT-04; D-37(b) | S | A-073, A-072 | Phase 8 |

### Phase 9 — scoped 2026-09-02 from the operator review at the Phase 8 close

> `docs/reviews/16-operator-review-phase-8-close.md`. **The same shape, one door further along, and this time the proof is inside ONE FUNCTION: A-075 changed what the push MOVES and never told the push's model of what the day OCCUPIES — the provider axis asks `PUSHABLE_STATUSES` (`push-column.ts:119`), the chair axis asks `ACTIVE_STATUSES` (`push-column.ts:372`), 253 lines apart.** Three of the five findings were proved by RUNNING code against the local database, with transcripts in the review. Row 80 is built FIRST despite being second in the review's own ranking: it is under an hour and row 81's error path depends on it. **The reviewer verified D-46 end to end and it holds** — nothing anywhere derives attendance from silence. The process note is now a CLAUDE.md rule: *a narrower list is a new fact.* Finding 3 — the interface — is the design track rather than an `A-` row, and the brief already carries it.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 79 | ✅ A-078 | **The error mapper knows two constraint names and one error shape; the database has three names and two shapes — so the sentence D-45 promised the desk is a raw stack trace.** D-45 says it in the decision log: *"the constraint refuses it the moment anything has been sold, and the desk is told so IN WORDS."* It is not. Verified by RUNNING D-45's own scene — no-show released, she walks in, Priya squeezes her into the one chair, and both the un-release (`release-time.ts:261`) and the APPT-06 correction behind it (`transition.ts:134`) return `PrismaClientUnknownRequestError` with `mapped SlotTaken=false`, raw message `exclusion constraint "appointment_resource_body_no_overlap"`. Two causes, both in `errors.ts`. **(a) `OURS` (`errors.ts:65`) lists two names; A-063 added a THIRD and the mapper was never told** — confirmed against `pg_constraint`, which returns `appointment_block_no_overlap`, `appointment_resource_body_no_overlap`, `appointment_resource_no_overlap`. The only test touching it (`shared-chair.test.ts:182`) asserts the RAW error, which is why nine items went by. **(b) A DEFERRED violation is a different error shape with no SQLSTATE at all** (`isSlotTaken=false has23P01=false`), and `push-column.ts:558-560` is the only place that defers — so **`push-column.ts:677`'s catch has never once fired** and A-034's mapping has never worked. Eight call sites share this function, including `reassign.ts:155`, the sick-stylist bulk move. Scope: `OURS` derives from ONE list of our constraint names in the module that owns them, plus a test asserting that list matches the constraints actually in the database — the way the status predicate already does; the message check accepts **the constraint name alone**, because a name of ours is sufficient evidence and the SQLSTATE is absent from the deferred form; one test per shape, both provoked for real (immediate, and deferred inside `SET CONSTRAINTS … DEFERRED`); and `shared-chair.test.ts:182` stops asserting the raw string and asserts the mapped error, which is what the desk actually meets. *Under an hour, and row 80 depends on it* | D-2, D-45; RES-02 | S | A-063, A-075 | Phase 9 |
| 80 | ✅ A-079 | **The column push plans against only the rows it is MOVING, so the rest of the column is invisible to it — and it is a 500 on Dana's worst hour.** Saturday, quarter past twelve, the ten o'clock never came and Dana has caught up: *"pull everyone forward twenty."* Verified by RUNNING it — a 13:00 that starts BEFORE `fromAt` and the 14:00 pulled back 30 gives `PULL PREVIEW canPush=true` then `conflicting key value violates exclusion constraint "appointment_block_no_overlap"`; and a terminal row standing mid-column gives the same crash with `AFTER: nothing moved`. Three independent layers: **(1)** `previewPush` selects (`push-column.ts:119-124`) with `PUSHABLE_STATUSES` **and** `startAt >= fromAt`, so everything else occupying the column — the visit still running, the no-show still holding ninety minutes (D-7), the one finished early — is simply not in `rows`, and the cascade's `staying` set (`:252-266`) is built from `rows.filter(r => r.problem)` and cannot see any of them. **D-26's promise that a left-behind appointment still occupies its old time is true only of appointments the push happened to select.** **(2)** The chair axis already gets this right (`:372` loads holds in `ACTIVE_STATUSES`) — one function, two axes, one of them told. **(3)** `confirmColumnPush` (`apps/web/lib/day/actions.ts:171-174`) catches `RangeError` and rethrows everything else, so even a correct mapping would still crash the day grid. Scope: the planner loads **everything occupying the provider's day in `ACTIVE_STATUSES` across the whole affected span** (`min(fromAt, fromAt + shift)` to the last shifted end), not just the movable rows; rows outside the move set enter the cascade as permanently immovable with their own `problem` word, so the preview says *"the 13:00 is still in the chair"* rather than promising a clean pull; immovable rows are measured from the **stored `blockedStart`/`blockedEnd`**, never re-derived as `endAt + bufferAfter` the way `push-column.ts:229` does — that line is a second copy of the blocked range and re-deriving would re-break A-069's cut; `confirmColumnPush` grows a `SlotTaken` arm with words. Test: **a stationary occupied row on BOTH sides of `fromAt`** — before it for a pull-forward, after it for a push. `push-chairs.test.ts` has no such fixture and there is no `push-column.test.ts` at all. *A failed pull-forward is not a lost booking, it is the desk deciding the push button is broken — and A-018, A-034, A-059 and A-075 all becoming dead code at once* | APPT-04; D-26, D-7 | S–M | A-078, A-075, A-018 | Phase 9 |
| 81 | ✅ A-080 | **DECIDED → D-47 (two hours past `endAt`, all three actual timestamps).** closing a `checked_in` visit on Monday writes a Monday finish time — D-46's own rule, not applied to the path its own screen drives.** D-46 is emphatic: a `completed` reached from `booked`/`confirmed` leaves both timestamps NULL, *"because a missing timestamp is honest and a Monday-morning one is a lie in the audit trail."* `transition.ts:330` then reads `from === 'checked_in' || from === 'in_progress' ? { endedAt: now } : {}` — and `/staff/unfinished` exists precisely to close `checked_in` rows days later, its own copy calling that the confident case (`unfinished/page.tsx:97-100`). So the primary path through Phase 8's new screen writes exactly the lie D-46 refused: `checkedInAt` Saturday 14:15, `endedAt` Monday 09:40, and the panel renders "Finished: Monday 09:40" against a Saturday appointment — a forty-three-hour visit in the one record meant to answer "when did this actually happen". **The rule was written for the TRANSITION when it is a rule about the CLOCK**: `now` is only a measurement while the visit is still plausibly happening. **OQ-20, answered as a D-number first: what is "still near the visit"?** (a) `now <= endAt + one hour`; (b) same business day as `startDay`; (c) leave it and accept the cost. Scope once decided: `endedAt` stamped from `checked_in`/`in_progress` only inside that bound — the same shape as the existing staleness rules, not a new concept — NULL beyond it exactly as the two new edges are, and the event still records who closed it and when. Test: close a `checked_in` row three days later, assert `endedAt` is null and **`checkedInAt` is untouched**, because the check-in time is a real measurement. *No report reads `endedAt` today; the harm is the record and the panel, and it poisons the only data that could tell the owner how long her colourist's colours really take* | APPT-03; **OQ-20 → D-47**; D-46, D-7 | S | A-076 | Phase 9 |
| 82 | ✅ A-081 | **DECIDED → D-48 (a widen-the-window control, the same shape as A-073's `weeks`; the toolbar badge deliberately keeps the 21-day default).** nobody has ever seen these screens on a full book, and the 21-day lookback is the only door to fixing the numbers.** Measured against a freshly reset and seeded database on 2026-09-02: `unfinished NOW: 0`, `opened up: 0`, `on today's book: 0` — and `unfinished as-if 2026-06-15: 166, value $3680.00`, against a seeded book of `booked=179 completed=43 no_show=3 cancelled_late=2` with **176 past-and-still-open rows**. **(a) The demo is dark.** The seed anchors to a fixed constant (`density-seed.ts:28`) — correctly, because a `now`-anchored seed makes the DST fixtures vanish in July — so twelve weeks on, every date-relative surface built in Phases 6–8 renders its empty state. The e2e specs pass because `e2e/fixtures.ts` TRUNCATEs and seeds its own rows: **this is CLAUDE.md's own "dormant on a fresh install" trap wearing a demo hat**, and it means no human has ever looked at `/staff/unfinished`, `/staff/opened` or the day grid against 227 appointments. **(b) 176 rows are open, the screen offers 0, and after 21 days there is no list at all.** `UNFINISHED_LOOKBACK_DAYS = 21` (`unfinished.ts:49`) is a sensible desk bound, but D-46's whole argument is that the reports become right *because the desk can tell them the truth* — and the only surface that lets it goes blind at day 22, permanently. Scope once decided: the seed grows a demo week anchored near `now` **beside** the fixed DST days, which stay exactly where they are — both idempotence rules hold (`seedSetup` twice identical, `seedDensity` refuses a non-empty book) — including deliberately unclosed past rows and a released no-show, so the Phase 7–8 features are visible on a fresh install rather than dormant. **OQ-21: what happens to rows older than the window?** (a) a widen-the-window control, the same shape as A-073's `weeks`; (b) an always-present "older than three weeks" collapsed section; (c) nothing. **No auto-anything — D-46 stands, and 176 unclosed rows is an argument for a wider door, never for a guess** | RPT-01; **OQ-21 → D-48**; D-46 | S–M | A-076, A-045 | Phase 9 |
| 83 | ✅ A-082 | **Demo checkpoint 6 — walk the Phase 9 boundary before scoping Phase 10.** Checkpoints 1–5 each found a defect sitting inside an item already marked ✅ and invisible from within the item that introduced it; the rule is to walk one when a milestone closes, not when convenient. Two of the four scenes were only runnable because A-081 had just lit up a 411-appointment book. Found ONE, and it is the offered-then-refused class for the fourth time: **the room's READ MODEL and the room's CHOOSER have asked different questions since A-032** — `fullSpans` asked *"are all four chairs occupied at some instant?"* and `findFreeResource` asks *"is there ONE chair free start to finish?"*. The first is necessary and not sufficient, because it is the answer you get if the room can be RESHUFFLED and a client stays in her physical chair. Three chairs busy at every instant and never four is a room that is never full and still cannot seat her: the public page offered the time and the write refused it with `NoResourceFree`, **permanently — not a race**, on 1.8% of offers over the seeded book. Fixed by deleting the cardinality model and mirroring the chooser's predicate line for line (`canSeat`), threading A-063's holder so the offer is not stricter than the constraint either, and routing all five offering surfaces through one `computeSlotsIn`. **Walked and found CLEAN: A-079's push (408 previews, 271 real pushes, zero disagreements) and A-069/A-074's release seam end to end.** Transcript: `docs/reviews/17-demo-checkpoint-6.md` | RES-03, BOOK-02; A-032, A-063, A-082 | M | A-081 | Phase 9 |

### Phase 10 — scoped 2026-09-02 from the operator review at the Phase 9 close

> `docs/reviews/18-operator-review-phase-9-close.md`. **The same shape one more door along, and this time it is inside A-082's own fix: the room's question gained an input — WHO would be sitting in the chair — and it was threaded through every caller that holds an APPOINTMENT (`reschedule.ts:449`, `change-services.ts:430`, `book.ts:208`/`:518`) and neither of the two that hold a CLIENT and no appointment yet, which is what a booking panel is. The offer is now STRICTER than the write on the desk's own screen, proved by running it: `13:45 offered without a holder: false ['no-resource-free'] / with her holder: true / the write accepted it: true`. The workaround is worse than the refusal — a BOOK-05 override holds no chair at all (D-30), so one wrongly refused offer becomes one client sitting in a chair the room believes is empty. Two of the four findings were proved by running code; two more hypotheses were tested and came back CLEAN and are recorded as such. And the interface is named for the FOURTH time — this row stops arguing for it and asks for a decision instead (OQ-22).**

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 84 | ✅ A-083 | **The desk names the client and then asks the room the anonymous question — A-082's fix with the sign flipped.** `canSeat` takes `holderKey` because A-063 lets one client's own overlapping envelopes share a chair; `null` is the strict question and is right for a visitor who has not said who she is. The desk's booking panel passes `null` for a client it has already resolved five lines above the call: `page.tsx:91` computes `prefillClient`, `page.tsx:95-96` calls `staffSlotsFor` without it, and `booking-panel.tsx:105`/`:158`/`:168` has the chosen client in state and does not read it. `anyProviderTimes` (`any-provider.ts:61-70`) has no client field AT ALL, so the "anyone" path cannot pass one. Measured on a two-chair room: **9 slots offered anonymously, 11 with her holder, and the write accepts the withheld one**. The desk is told *"every chair is taken then"* about a chair that is hers and empty, and the only way through is an override that by D-30 holds no chair — so the room's model of the day is then wrong for everyone else. **The regression test must be an AGREEMENT test** (the same offer computed twice, compared against what the write accepts), never two separate assertions — checkpoint 6's whole lesson. **Shipped with a FOURTH surface the row did not name and the defect did: `walkInOptions` asked anonymously too, and a named walk-in is usually a client already in a chair for something else. The panel also re-asks when the desk picks the client, because BOOK-04's order asks for her LAST — every list on the screen had been computed for nobody. The agreement test compares one offer against what the write accepts over every candidate the ROOM decided; reverting either thread fails it** | RES-02, 04; BOOK-04; D-30, D-17 | S | A-082 | Phase 10 |
| 85 | ✅ A-084 | **One room rule, three copies, and only two of them are pinned together.** `findFreeResource` (`resources.ts:42-110`, a Prisma `where` — what the write ACCEPTS), `canSeat` (`resource-load.ts:106-125`, a predicate over `ChairHold` — what the screen SHOWS) and the push's planner (`push-column.ts:592-596`, a predicate over its own row shape — where the push SEATS the column). The first two mirror each other deliberately and say so. The third is written as `E && (D \|\| B)` where the others are `(E && D) \|\| B`, which agree **only** where a body overlap implies an envelope overlap — true today because the hold trigger keeps the body inside the envelope on every branch including the release cut, guarded by a CHECK, in a migration three files away and asserted nowhere in TypeScript. Not a live defect; it is the precondition of checkpoint 6 — two correct-looking halves agreeing for a reason nobody wrote down. One shared pure predicate, three callers, plus one test that puts a room in a state and asserts all three answers are EQUAL. **Do not make the push call the database** — it plans a column in memory on purpose | RES-02; D-30 | S | A-083 | Phase 10 |
| 89 | ✅ A-088 | **The token layer and the type scale — deliverable §8.1/§5.1 and §5.2, and the first half of what D-49 blocked A-085 on.** Named by ROLE, never by value, in `app/globals.css` under `@theme inline`: ground (page / raised / sunken / overlay), ink (primary / secondary / muted / inverted), line (hairline / control / strong), the five intents (neutral, positive, attention, danger, information), focus ring, selection, disabled, a two-step radius scale, and one motion duration+easing pair. **Derived from the audit, not invented:** the staff app today is zinc + amber (80 text uses — running late, no-show, override) + red, ONE radius (`rounded-md` ×158), ONE shadow use in the entire app (so it is flat and the scale says so), and three type sizes carrying everything (`text-sm` ×347, `text-xs` ×89, `text-2xl` for titles). Tokens must equal today's audited values wherever one exists, so that A-089+ applying them is visually a no-op and axe cannot regress. **The public site's palette does NOT fold in** (§5.1) — name the shared concepts so a third surface can pick a side. **The stated contrast result is a COMPUTED one:** a test that reads the tokens and asserts each against its own ground at its real bar (4.5:1 text, 3:1 graphical), because a contrast number written in a comment rots the first time a hex changes. `--color-muted-ink`'s existing header comment is the precedent — it exists because half the marketing pages passed axe and half did not. The one genuine GAP the audit found: **three explicit focus styles in the whole staff app**, everything else browser default, against §4's "every interactive control has a visible focus ring" | Design brief §5.1, §5.2, §8.1; D-49 | M | — | Design pass |
| 90 | ✅ A-089 | **The primitives the shell actually uses — a SUBSET of §5.3, and the subset is the point.** `Button` (primary / secondary / quiet / destructive × pending, because every mutating control here is `useActionState` and has a `pending` boolean), `Link`-as-button, `Badge` (the three shell counts), `Field`+`Input` (the client search box), `Tabs` **as links with `aria-current`, never JS state — these are URLs**, and `EmptyState`. **Native first, per §5.3 and the house instinct:** the desk switcher is already a `<details>` and stays one; nothing here may add a client-side UI dependency (§9). Everything else in §5.3 — Select, Textarea, Checkbox, Radio, Card, Table, InlineAlert, Sheet, Toast, Skeleton — waits for the screen that needs it, because a primitive with no caller has no state matrix anybody has checked. Each one ships with its states drawn and an axe pass. **Built 2026-09-04: six primitives, plus `/staff/design` — the state matrix as a page, which is what gives six brand-new components a caller, an axe run and a keyboard before A-085 puts them on every screen (the row's own argument, applied to the row). `Field`'s children is a render prop so the label/hint/error ids are ONE fact rather than two copies; its error slot keys on `undefined` vs `''` because a live region must exist before its text does. `pending` IS `disabled`. The current `Tab` carries a print override even though its one caller is `print:hidden` — a primitive correct only because of where its caller sits is a defect waiting for the second caller. 44px is measured (`boundingBox().height`), not grepped. Guarded by `packages/design/primitives.test.ts`: no raw palette, no `dark:`, no hex under `components/ui`** | Design brief §5.3, §8.2; D-49 | M | A-088 | Design pass |
| 86 | ✅ A-085 | **DECIDE FIRST (OQ-22 → a D-number), THEN BUILD: the staff shell, named for the fourth time.** `app/staff/layout.tsx:22-27` still renders one piece of persistent chrome, the desk-switcher bar; `/staff` is twelve links in a column (`staff/page.tsx:36-74`) and names twelve of the twenty-three staff routes. Counted inbound links: `/staff/unfinished` has **one door**, the day toolbar, behind a badge that hides at zero — Phase 8's headline screen is invisible to a desk that has never had an unfinished appointment. `/staff/opened` and `/staff/conflicts` have two each; `/staff/waitlist`, `/staff/messages` and `/staff/clients` have one each, `/staff`. And there is still no route to a client from the day while the phone rings. The specification already exists and is not to be re-derived: `01-design-brief.md` §5.5 — the three badges (*Opened up*, *Still open*, failed messages), the day grid as the real home screen, no hamburger, tablet landscape, owner surfaces visibly a different tier — plus the client search box the brief names. **DECIDED 2026-09-04 → D-49: it WAITS.** The owner chose (b) over the reviewer's (a). This row does not move until the design deliverable lands (`01-design-brief.md` §8.1 tokens, §8.2 primitives, §8.4 the shell at tablet-landscape and laptop widths), and until then D-49 forbids the partial version arriving as a ride-along — nothing adds persistent chrome to `app/staff/layout.tsx`. The three costs are accepted and written down in D-49, not forgotten. **Built 2026-09-04 → D-50 answers the three things §5.5 left open: `/staff` becomes the SETUP index and the day grid is home (sign-in and the non-owner redirect both go to `/staff/day`, eleven `← Staff` back-links deleted); ONE wrapping nav row with the owner tier after a rule, no hamburger; and THE DOOR IS PERMANENT WHILE THE BADGE HIDES AT ZERO, which settles A-076 against D-49 rather than picking one. The three counts are OWNED by the layout and `/staff/day` no longer computes any of them — `listOpenedSlots` is a derivation, not a `COUNT(*)`, so a second copy would be the checkpoint-6 class. Setup stays `requireStaff`: `requireOwner` redirects there, and a navigation change must not remove a stylist's access to Services (D-36)** | Design brief §5.5, §6; **D-49**, **D-50** | L | **A-089** (§8.1 + §8.2 landed) | Design pass |
| 87 | ✅ A-086 | **The last hand-typed status list.** `reports/dashboard.ts:87-91` types `status: { in: ['completed', 'no_show'] }` at its call site; every other reader in the product derives from the status module (`lapsed.ts:134` `MISSED_STATUSES`, `opened.ts:202` `SLOT_FREEING_STATUSES`, `resources.ts:91` `ACTIVE_STATUSES`). Right today, wrong the day a ninth status arrives, and CLAUDE.md's first structural rule. Flagged at the Phase 8 close as *"one constant, next time the file is open"*; Phase 9 never opened the file. Two lines — ride it along with any item that opens the reports directory. **Built 2026-09-04: it was TWO call sites, not one — `dashboard/page.tsx:104`'s utilization drill-down link carried its own copy of the same pair, so the tile and the list it opens were free to disagree. `CONSUMED_STATUSES` is derived (terminal AND active), not typed, and pinned by `status.test.ts`** | RPT-02; D-7 | XS | — | Phase 10 |
| 88 | ✅ A-087 | **Demo checkpoint 7 — walk the product through the new shell.** Checkpoints 1–6 each found a defect sitting inside an item already marked ✅ and invisible from within the item that introduced it; checkpoint 6 needed a busy room to see anything at all, so this one walks the seeded density book too. A-085 changes navigation on every staff screen, which is exactly when a walk pays: the four scenes to re-walk are the six-o'clock close-out (does the desk find `/staff/unfinished` without the badge?), the phone call about Mrs Kerr (client from the day, in one hop), the freed-Saturday sale (`/staff/opened` → waitlist → book), and a Saturday afternoon add-on for a client already in a chair (A-083's fix, on a real book rather than a two-chair fixture). **Walked 2026-09-04 → `docs/reviews/18-demo-checkpoint-7.md`. ONE DEFECT, FOUND TWICE IN TWO UNRELATED FILES FOR THE SAME REASON: every axe run in this suite has looked at an empty screen.** `/staff/day` fails AA on every chip somebody has CLOSED OUT — `completed` paints a darker ground AND a lighter ink and the detail line then multiplied that ink by `opacity-80`: 4.33:1 light, 4.18:1 dark. A-088's contrast guarantee is a COMPUTED one and an opacity is the same fact written again under another name — it changes the colour the eye receives without changing the token, so every contrast assertion still passes. `day-grid.spec.ts` runs axe on that page and is green because it seeds ONE `booked` appointment, whose white ground makes the same 0.8 scrape past at 4.66:1. The sibling: `/staff/clients/[id]`'s section headings are 2.62:1 on white, and that page's axe test opens a client who has never been in, so neither heading renders. Also: no spec had ever run axe in the DARK scheme. Fixed with a guard (`packages/design/opacity.test.ts`, no unprefixed `opacity-*` under `app/staff`), both axe fixtures given the states their screens actually render, and the day grid asserting both schemes. Plus two shut doors: her name on the appointment detail is now the link to her record (the only one had been the reliability flag, so a client in good standing had none), and the bare dashboard drill-down says "Pick a week" rather than "0 appointments". **Walked CLEAN:** 25 staff routes all 200 with no errors, badge == list, and 201 offers booked with 0 refused on a staggered four-chair room (3 slots widened by naming the client, 0 narrowed). **Scene 3 could not be walked — A-095** | — | M | A-083, A-085 (**so: blocked behind the design pass, per D-49**) | Phase 10 |
| 92 | ✅ A-090 | **`AppointmentChip` and the day grid, composed — §5.4.1–3 and §8.5.** The atom of the product: client name, service, status, override marker, per-visit note, reliability flag, and — running late — the booked time AND the projected time, all inside a 45-minute chip at 1.5px/min. Four compositions per §8.5: four stylists, one stylist, a column forty minutes late, a day with a stylist off. **The two times are the risk** (§4): visually distinguishable at a glance and unambiguous in the accessible name, or the paper book goes back on the counter. **Built 2026-09-05. THE FINDING WAS THE GRID ITSELF: its columns never shared a vertical axis.** Each laid out its own heading and controls before its positioned box, so minute zero sat wherever that column's chrome ended — and the chrome's height depends on the DATA. Measured on a seeded Tuesday with nothing running late: gutter 380, Dana 480, Priya 480, Marcus 438, Tess 438 — the hour labels **67 minutes** above the rows they label, and the two stylists with quieter columns drawn **28 minutes above** their colleagues, because a column with nothing left to push renders no push control. "Who is free at two?" is read ACROSS, and it was wrong by half an hour, differently per column. Fixed with one two-row grid and `grid-rows-subgrid` per column, so the tallest chrome sets row one and every day starts on the same pixel; `RoomStrip` had it identically (an out-of-service chair's heading wraps) and got the same fix. Guarded by an e2e that makes one column's chrome genuinely taller and asserts every `data-day-axis` shares one top, with a count guard so it cannot pass vacuously — verified by mutation. **Second defect:** the projection hand-typed `status === 'booked'`, a status list of ONE and narrower than both neighbours, so a CONFIRMED client was on the ring-round as "likely 14:40" while her chip showed no projection — `AWAITING_START_STATUSES` is the third list and the three now nest (ring ⊂ project ⊂ push). **Third:** four of eight statuses were told apart by colour alone, two of them by adjacent tints of one hue; every non-default status now carries a WORD that survives truncation | Design brief §5.4, §8.5 | L | A-085 | Design pass |
| 93 | ✅ A-091 | **`/staff/opened` and `FreedSlotRow` — §8.6.** All five `freedBy` kinds visible at once, one already carrying two call marks. Brings `CallMarkButtons` with it (§5.4.9) — the most-reused interactive control in the product, and the one with no design at all. **Built 2026-09-05. THE FINDING WAS ITS SIZE: 26px, against §4's 44px desk target**, on the control the desk presses more than any other — and the undo beside it, an underlined `text-xs` link, was SMALLER THAN THE THING IT UNDOES. A mis-tap on a shared screen marks the WRONG client as rung and silently skips her, which is the exact harm the undo exists to reverse. **Nothing in the gate could see it:** 44px is WCAG 2.5.5, which is AAA; the AA rule is 2.2's 2.5.8 at 24px, which 26 passes and which CI's four tags do not run — so the brief's bar is one no tool in the suite can check, and the guard has to MEASURE a rendered box. **Second:** the pressed state was not in the accessibility tree at all, and both copies carried a comment saying `aria-pressed` "is not available on a submit button that is also the form's payload" — untrue (a submit button has role `button`, which supports it), so the state was an inverted ground and nothing else: colour alone in the tree as well as on screen. **Third, and why the first two were each wrong twice: it was never one component.** §5.4.9 said *one component, three surfaces* from the start; A-061 wrote it for the call-down, A-072 wrote it AGAIN for the waitlist with a copy of the same eleven-line style function, and A-073 wrapped A-072's while noting a third copy would be wrong. Three files deleted, one component, parameterised by the only three things that differ. **Fourth:** five user-facing sentences asserted a gender the `Client` record has no field for ("Mrs Hall dropped **her** Colour", "**She** took it", "**She** never came, and 45 minutes of **her** slot are still blocked") — on screens the desk reads down the phone, in a salon with male clients | Design brief §5.4, §8.6 | M | A-085 | Design pass |
| 94 | ✅ A-092 | **`/staff/dashboard/lapsed` — §8.6a.** The only screen in the product a person works *down*, thirty rows long, each row a phone call waiting to be made. The row has to survive that length. **Built 2026-09-05. THE FINDING WAS THE NUMBER ITSELF: 16px tall and 3.9px from a link that navigates away**, on the one control every row on this screen exists for, against §4's 44px desk bar — measured at thirty rows on a 1024×768 tablet, where the list is **5044px, 6.6 screens**. The mis-tap does not cost a tap; it lands on the client record, leaves the list and returns to the top of it with no memory of which of the thirty rows had been reached. **A-091 fixed this exact defect one item earlier and its own note undercounted the rest**: it left behind *"the `tel:` links on the waitlist, lapsed and call-down rows are still inline `text-sm`"* — three — and a grep says **eight**, adding the appointment detail, the stranded list on Providers, the conflicts list, the day column's ring-round and `/staff/opened` itself. A list of known copies written from memory is the same defect one level up. `PhoneLink` is one component with eight callers, `phone-link.test.ts` walks `app/staff/` so a ninth cannot be typed, and it asserts the callers still exist so the rule cannot point at nothing. The href was broken too: every staff copy interpolated the stored string into `tel:`, so "(512) 555-0188" dialled nothing. **Second:** the report failed AA in dark (`text-zinc-500`, 4.1:1 on #0a0a0a) and its axe test could not see it — light only, and ONE row with NO call mark, so the half of the row that only exists once somebody has started working the list had never been rendered under axe. Both schemes now, over a list carrying a fresh mark and a stale one, reloaded between. **Third:** 30 rows × 5 call-mark buttons is 150 controls drawn from five words and none named its client — A-091 put the pressed STATE in the tree, the SUBJECT was still missing on all three list surfaces. **Plus, unrelated and pre-existing:** `booking.spec.ts`'s A-071 case fails on the CLOCK — the first slot left at 09:56 was 12:00, the only slot of the day exactly one stylist can take (the other three break 12:00–13:00 and a 45-min Cut runs in), so the product refused correctly and the test measured the hour. Pinned to a whole open day; **the product gap it exposed is A-097** | Design brief §8.6a | S | A-085 | Design pass |
| 95 | ✅ A-093 | **The print sheet in greyscale — §8.7 and §5.4.4.** Not a styled `DayGrid`; a different document. One column per page, a blank scribble column, per-visit notes visually distinct from pinned client notes. Anything that only works because of a background colour is invisible on paper. **Built 2026-09-05. THE FINDING WAS UNDER THE SHEET: the day view collapsed a SEGMENTED visit to its LAST block, so a 09:00–11:00 colour was drawn on the grid at 10:25, 55 minutes tall, and the hour the stylist spends applying it showed as empty column.** `findBusyAppointments` returns one row per worked block keyed by the APPOINTMENT's id — the whole mechanism of D-29 — and `day-view.ts` joined it back with `new Map(busy.map((b) => [b.id, b]))`, which is last-wins. Nothing saw it because the collapse keeps the range's END: gaps are subtracted from the busy spans and stayed right, the printed times read `startAt`/`endAt` and stayed right, the engine never reads it. **Only the START moved, and only on a segmented service, which no fixture in the suite had ever created and which is the salon's most valuable one.** Found by the PAPER, where a table says with a SORT what a grid says with a position: a 09:30 override printed ABOVE the 09:00 colour it overlaps. **Second: a list cannot say "at once".** Only an override can put two clients in one stylist's hour (D-8's zero-width range is what gets it past the constraint), and on paper they are consecutive rows — the shape of SEQUENCE. §5.4.11's marker was on the chip and on none of the eight pages the stylists hold; it is on the paper now with the reason somebody typed. **Third: the sheet's own comment calls it "who is coming" and it printed the one client who definitively is not** — `occupiesTime` deliberately keeps `no_show` (D-7), in the same ink as everybody else, and A-069's released time had nothing to separate the no-show from whoever bought her hour. One defect three times, so the guard is drawn at the shape: `sheet-parity.test.ts` — every `item.<field>` the chip reads is on the paper or written down with its reason, and no exemption may name a field the chip no longer reads. **Fourth, print-specific: the file's own comment swore the date is on every page and it was not.** A browser repeats `<thead>` and nothing else, so page two of anybody with more than a page of clients carried the column headings and no name and no date — proved on a real PDF (Priya's fourteen clients, breaking after 11:40), and it needs a full day to exist at all where every fixture seeded one appointment. **Plus:** "Not working today." told apart from "Nothing in the book.", the hours in words, the scribble column no longer headed "Notes", and `border-black` → tokens (invisible on the dark tablet the sheet is previewed on) | Design brief §5.4, §8.7 | M | A-088 | Design pass |
| 96 | ✅ A-094 | **The mobile public booking flow — §8.8.** The client's one shot, on a phone, at night, and she will not see it again for six weeks. **The public palette stays as it is** (§9) — this is composition and density, not a re-identity. Never leak why a slot is unavailable | Design brief §8.8 | M | A-088 | Design pass |
| 97 | ✅ A-095 | **The demo book's dark corners — what checkpoint 7 could not walk.** `seedDensity` fills only Dana (0.7) and Priya (0.4) into the `now`-anchored window, so **Marcus and Tess have no future appointments at all**, two of four columns are empty from today onwards, and on every day of the book the number of chairs in use equals the number of stylists working — the room axis never binds and a read-only sweep of the future book found **21,184 offers with not one `no-resource-free` refusal**. Checkpoint 6's entire finding needed a busy room to exist. And there are **zero waitlist entries, zero call marks and zero call-down attempts**, so `/staff/opened` → "Who wants this slot?" → *"Nobody on the waitlist fits this one"*: WAIT-01–04, A-021, A-023, A-043 and A-072 all render their empty state on a book with 453 appointments in it. A-081's own trap, one layer down from where A-081 closed it. **Not a ride-along:** the seed is pinned by an every-column idempotence test (A-045) and by A-024's exact utilization constant over `DEMO_WEEK`'s whole ISO week, so both have to be satisfied deliberately **Built 2026-09-06. THE ROOM AXIS WAS ONE LINE, AND THAT IS THE FINDING:** adding Marcus and Tess to the moving book took the future book from **2 concurrent chair holds against 4 chairs** to 4, and the same read-only sweep from **0 `no-resource-free` to 39**. The room could not bind because the number of chairs in use never exceeded the number of stylists working — checkpoint 6's entire finding was unreachable on the demo book and would have stayed unreachable however many appointments the seed wrote into two columns. The refusals cluster on the SHORT services, which is the operator's own case: a ten-minute fringe trim fits in the stylist's gap and there is no chair to put her in. **THE SHARPEST DEFECT WAS IN THIS ITEM'S OWN FIRST DRAFT:** a waitlist entry derived from "whichever span opened up" counted 1 and MATCHED NOBODY — `/staff/opened` carries A-069's RELEASED TAIL as well as A-043's cancellation, and a tail is 25 minutes of a two-hour colour whose `primaryServiceId` is still the colour, so `matchFreedSlot`'s footprint arm refused it. Correctly, and silently, and **`waitlistEntry.count() > 0` passes against that** — the list is there and says *"Nobody on the waitlist fits this one"*, character for character what it said with no entries at all. Derived from the CANCELLED row instead, the one span whose freed minutes are the whole footprint by construction, and the test asserts what the SCREEN says. **Plus one the row did not name:** `/staff/dashboard/lapsed` (A-092, thirty rows, built one item earlier) had never carried a row and could not — "nothing booked ahead of her" is half the definition and all eight demo clients are booked, so MORE APPOINTMENTS MAKE IT WORSE. Five history-only clients the moving book cannot reach, their gaps measured from `LAPSED_WEEKS` rather than typed. Also 3 waitlist entries (one guaranteed match, two narrowed so the filters have something to exclude), call marks on BOTH subjects, 2 call-down attempts in BOTH outcomes. A-024's frozen `1290/2100` is untouched — everything added runs strictly after the fixed book in the PRNG stream | A-081; checkpoint 7 | M | — | Phase 10 |
| 98 | ✅ A-096 | **Dark-scheme axe across the rest of the staff app.** Playwright's default colour scheme is light and no spec had ever changed it, so on a palette built to flip with `prefers-color-scheme` half of it had never been measured. Measured at checkpoint 7 on the seeded book: **124 nodes on `/staff/clients/[id]`**, 2 on the appointment detail, 2 on the dashboard drill-down — **one value repeated**, `text-zinc-500`, which is 4.6:1 on white and **4.1:1** on `#0a0a0a`. The day surfaces are already swept and `day-grid.spec.ts` asserts both schemes; this is the same sweep for the rest, and it should ride along with A-090–A-094 rather than run as a pass of its own, because those items retire the same screens onto the tokens. **`emulateMedia` alone is not enough** — it switches the media query on a live page and axe then samples every control mid-`transition-colors` (583 nodes of blended colours). Reload **Built 2026-09-06. THE FINDING WAS THE SIZE OF IT: 275 nodes across TWELVE staff routes, all one value** — `text-zinc-500` (#71717b) at **4.1:1 on #0a0a0a**: 194 on the client record, 27 on services, 23 on availability, 12 on settings, 4 each on the dashboard and waitlist, the rest in ones and twos. **Light was clean on all twelve**, which is the entire mechanism. **The colour was one substitution; the BLINDNESS is the item.** A light-only axe call is not a bug in one spec, it is the default shape of the assertion — A-092 and A-094 each found this on their own screen and each fixed it on their own screen, leaving thirty-eight specs to write it the same way tomorrow. `e2e/axe.ts` now owns the scheme loop for all **41** assertions and `no-restricted-imports` bans `AxeBuilder` from `e2e/**/*.spec.ts`, so the door is shut rather than the three noticed rooms swept. **It FREEZES transitions rather than reloading**: measured both ways on four routes, the naive flip invents 10 phantom nodes and freeze-vs-reload agree exactly — and the reload was throwing away the state each test had built, which is how an assertion ends up scanning a page nobody set up. **A-096's own first sweep did exactly that**: 46 routes reported clean were 46 renders of the login page behind a sign-in that had silently failed. Also verified `text-ink-muted` EXISTS in the built CSS — an ungenerated class would have passed contrast too, by rendering muted text as primary. **Left behind:** `/staff/dashboard/overruled` has no e2e spec at all (its fixture needs a real overruled cancellation; an empty-list scan would pass for the wrong reason), and `/staff/design` carries 50 axe `incomplete` results nothing asserts on | Design brief §4; checkpoint 7 | M | A-090 | Design pass |
| 99 | ✅ A-097 | **"No preference" dead-ends her after all, when nobody else is free at that instant.** A-071's whole point is that a client who said she does not mind must never be sent back to the time list — "sorry, pick again" is where a first-time client leaves. It re-offers the same time with somebody else, and **when there is nobody else it falls back to the VANISHED STYLIST'S day**, which is empty by construction: she is off. Proved by running it on 2026-09-05 at 09:56 — the flow picked Tess at 12:00 (the only slot of the day one stylist can take, the other three being on their 12:00–13:00 break), Tess was taken out from under the page, and the screen answered *"No appointments available that day. Please choose another day."* on a Saturday with three stylists free from 13:00 and an empty book. **She said NO PREFERENCE and was shown one person's empty column** — the fallback carries a provider she never chose. Found by A-092 because the pre-existing spec failed on the clock;  the spec is now pinned to a whole open day, so nothing covers this path **Built 2026-09-06. THE DEFECT WAS ONE ARGUMENT — AND IT WAS THE ONE SHE NEVER SUPPLIED.** `anyProviderAt` returns `null` when nobody qualified is free at that instant, and `confirmAppointment` then fell through to `listTimesOn(serviceIds, input.providerId, day)` — on this path a stylist SVC-02 ASSIGNED her, whose name she first heard on the details screen, and whose day is empty by construction precisely BECAUSE the reason `instead` came back null is that she has gone. **Half of A-071 was already right, which is why nothing caught it:** the `instead` arm asks across everybody and only the fall-through does not — two arms of one `catch` forty lines apart answering one operational question two ways, both returning `OfferedTime[]`, so the compiler cannot see it and the wrong one is POPULATED AND PLAUSIBLE on any day the vanished stylist is merely busy rather than absent. **The fixture is the item.** On a book where everyone works the same hours the two questions return the same list, which is why the pinned A-071 spec ("a whole open day, every qualified stylist free at 09:00") could never reach it: the new spec builds the opposite — the three seniors start at ten, so 09:00 is Tess's ALONE and 10:00 onwards is theirs — then takes Tess off sick mid-form, and the two questions answer NOTHING and SEVEN HOURS. It asserts its own precondition (`with Tess` on the heading) so a fixture that stops producing a one-stylist instant fails saying so. **Second, the same reader written twice:** A-071 fixed the details heading to `time.providerName ?? provider.name` and left the `done` screen on `provider.name`, so every client who ever booked this way was told **"Cut with No preference"** on the one screen she keeps — the A-056 spec asserts the confirmation heading and stops. **Plus, and not about this repo:** three consecutive gate runs failed in `density-seed.test.ts` and the seed measured 117.6s against a budget written for four; the one-line raise was the WRONG read — `pg_stat_activity` showed eleven connections against `countertop_test` and another project mid-sweep at load 28.9. A wall of timeouts in one file is an environment symptom — re-measured quiet at 76.9s for a file that seeds three times, so the 120s budget STAYED and the one-line raise was reverted | BOOK-01; A-071, A-056 | S | — | Phase 10 |

### Phase 11 — scoped 2026-09-07 from demo checkpoint 8 and the operator review at the Phase 10 close

> `docs/reviews/19-demo-checkpoint-8.md` and `docs/reviews/19-operator-review-phase-10-close.md`. **Two reviews, two independent halves of one shape, and the operator's half is the expensive one.** The checkpoint walked a book that finally pushes back — A-095's four columns, 44 `no-resource-free` refusals where checkpoint 7 had 0 — and found the offered-vs-accepted property **clean over 1,609 offers** and every one of 50 route×scheme pairs axe-clean. What it found instead was three screens that *answer a question nobody asked*, in the reassuring direction. The operator, given the same book, found the thing no walk would reach: **untick a stylist and her entire forward book leaves every screen in the salon while the clients keep getting reminders** — 106 appointments and $2,870 on a nine-day demo book, proved by flipping one boolean. Four filters, one line each, each correct in isolation and each carrying a comment justifying it; the right answer already exists one axis over at `room.ts:116`, where a retired **chair** keeps rendering until its last hold ends. Rows 100–102 are the operator's top three and are all `M`. **Nothing here re-opens a settled decision**; row 103 needs a NEW one and says so.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 100 | ✅ A-098 | **A stylist off the roster takes her whole forward book off every screen, silently, while the salon keeps telling her clients to come.** `provider.active = false` is the one action the product offers for "she has left", and A-041 does the right thing at that moment — it refuses the first click and lists everybody still booked, with phone numbers. **That list is the last time anyone in the salon sees those clients.** Four readers filter her out and each is defensible alone: `day-view.ts:144` (`active: true`) removes the COLUMN rather than emptying it, and the printed sheet renders the same `GridModel`, so they are off the paper too; `conflictsForDay` (`impact.ts:361`) derives conflicts from resolved hours and absences and deactivation writes NEITHER, so **nothing is ever stranded**; `unfinished.ts:94` and `opened.ts:217` carry the same filter with comments explaining why, so her past visits can never be closed out (permanently poisoning `dashboard.ts`, `lapsed.ts` and `reliability.ts` — the exact harm D-46 and A-081 exist to end, arriving through a door neither touched) and a Saturday she frees never reaches the screen whose job is selling it. Meanwhile `reminders.ts` has no provider filter — correctly, a reminder is about an appointment — so the client is told "your appointment is tomorrow at 4:30", taps the manage link, and the engine offers her an **empty day, forever** (`windowInstants: []`, `candidatesConsidered: 0`, same link, only the boolean changed). Proved on `db:reset:test`: deactivating Tess removed **106 future appointments worth $2,870**, left **106 chairs still held**, and `/staff/conflicts` reported **0 stranded**. **The answer is already in this codebase one axis over** — `room.ts:116` is `.filter((r) => r.active || r.holds.length > 0)` and A-046's dialog says it out loud: *"they keep the chair until they are done, and nothing new is seated here."* A retired stylist must render until her last appointment ends, be closeable, be conflictable and be sellable; she just must never be BOOKABLE. **Grep for readers of `active: true` across every surface, not the four named here** — that is this item, and CLAUDE.md's status-enum rule says the list is what to fix, not the four call sites | AVAIL-05; A-041, A-046; D-46 | M | A-041 | Phase 11 |
| 101 | ✅ A-099 | **The knowingly double-booked hour draws one client on top of the other, and D-8 promises in writing that it does not.** D-8's last clause is a promise about a screen: an override writes `blockedStart = blockedEnd` plus `overriddenFromRange` *"so the constraint never lies and **the day view renders the true collision**."* It does not render it. Two clients, same stylist, same instant, the second booked through BOOK-05 with a typed reason: both chips compute `top=60min height=55min`, and the horizontal extent is not per-item — `CHIP_SHELL` (`appointment-chip.tsx:89`) is `absolute inset-x-1` for **every chip in the product** and `GridItem` (`view-model.ts:23`) carries only `top` and `minutes`. **There is no lane, no offset and no width anywhere in the day surfaces.** The later chip in DOM order paints over the earlier one, opaque, `overflow-hidden` — so **the client who was already in the book is the one who disappears**, and the chip left standing wears the override marker, which reads as one deliberate override rather than as two people at ten o'clock. The desk overrides *because* it intends to see both. Needs lanes in the view model (n overlapping items share the column width) and it must hold on the printed sheet, where there is no z-order to hide behind | BOOK-05; D-8 | M | A-090 | Phase 11 |
| 102 | ✅ A-100 | **An absence longer than one day makes `/staff/conflicts?day=` lie on every day of the year.** Dana is off all week with flu; the desk writes one `TimeOff` row and follows the link A-041 hands it. `conflictsForDay` (`impact.ts:361`) loads **every `TimeOff` and `AdHocBlock` the provider has ever had, with no date predicate of any kind**, under a comment claiming the list "is scoped to one already-known day". Five clients on five different days show identically on `?day=2026-09-09`, `?day=2026-09-11`, `?day=2026-09-15` **and `?day=2027-03-15`** — still all five a year later, every one `completed`, and every row labelled with a bare `"09:00"` that does not say which day it is. So the second person to work the list on Wednesday re-rings the five already sorted on Tuesday, which is precisely the harm A-019's `conflictAckAt` was built to prevent, arriving by a route the acknowledgment cannot reach. Two halves: the absence query needs the day's bounds, and a row that can span days must print its DATE, not just its time | AVAIL-05 | M | A-019 | Phase 11 |
| 103 | ✅ A-101 | **DECIDE FIRST (a new D-number), THEN BUILD: the owner's headline number reads 0.0% on the week the dashboard opens on.** `/staff/dashboard` on the current week: **Bookings 157, Utilization Dana 0.0% Priya 0.0% Marcus 0.0% Tess 0.0%** — same card, same function, same seven days. **RPT-02's formula is frozen and is NOT in scope**: `Σ{completed, no_show} ÷ Σ(working − breaks − time off)` is implemented exactly, `dashboard.test.ts:95`/`:101` assert it deliberately, and the denominator is non-zero so `0.0%` is spec-correct. What nobody ever specified is **which week the tile is shown for**: a retrospective formula on a dashboard that defaults to the current week reads 0.0% for every stylist on every week that has not happened yet, and it sits directly above *"who to ring to fill a quiet Tuesday →"*, an action about the future. **0% and n/a are different facts and the code says so in a test name** (`dashboard.test.ts:95`) — and the product then uses `0.0%` for both "we had the hours and sold none" and "it is Monday". Invisible because the only week ever asserted is DEMO_WEEK, a **fixed past week**, the one kind where the retrospective answer is right; even that week reads 0.0% for three of four stylists and the constant test asserts only Dana. Decide: a second forward number ("how much of this week is sold", from `ACTIVE_STATUSES`), or the tile says *"not yet worked"* for a week still ahead, or both. Then build it and assert a FUTURE week | RPT-01, RPT-02 | M | A-024 | Phase 11 |
| 104 | ✅ A-102 | **The perishable-supply screen cannot see the biggest source of perishable supply.** A client no-shows a 120-minute colour; that time is still blocked (D-7 — `no_show` occupies its time, correctly, for the record and for utilization), and `/staff/opened` shows **0**. The release exists (A-069's `releasedAt` cuts the blocked range) but its button lives on one screen, while `no_show` is markable on three. The desk that taps no-show from the day grid at 10:15 has freed two hours nobody will ever be offered — on the one screen whose entire subject is money the salon would otherwise lose | APPT-01; A-069, A-043 | S | A-069 | Phase 11 |
| 105 | ✅ A-103 | **The walk-in nobody is free for is a dead end, and she is the walk-in worth the most.** The search is one day and there is no override path off the end of it — so the answer to "can you fit me in?" when the book is full is a blank screen, on the customer standing at the desk with cash. Staff can always act (START-HERE): the refusal needs the next-available across the following days and the RES-04/BOOK-05 override beside it | BOOK-04, 05 | S | A-017 | Phase 11 |
| 106 | ✅ A-104 | **Empty states that answer a question nobody asked — the shape, not the two screens that noticed.** `/staff/dashboard/overruled` reached without `from`/`to` says *"Pick a week from the dashboard."* **and**, in the next line, *"None that week — every cancellation was classified by the cutoff."* It reassures you about a week you have not chosen, and the reassuring sentence is a claim about an empty query. **A-087 fixed this exact construction one door along** — `dashboard/appointments/page.tsx:64` is `{fromDay && toDay ? 'Nothing matches this filter.' : 'Pick a week from the dashboard.'}` — and the sibling was never opened, because it is **the one staff route with no e2e spec at all**. `/staff/book` is the same shape: no `provider` param renders *"That stylist is not on today."* (`book/page.tsx:120`, branch `!walkIn && !anyone && !provider`), said on a Tuesday when all four are in, and the same branch catches a **deactivated** stylist, who is not "not on today" either. Audited repo-wide at the checkpoint: of the four parameter-driven zero-row screens, `clients` and `dashboard/appointments` are right and these two are wrong. Brings the missing `overruled` e2e spec with it — the fixture needs a genuinely overruled cancellation, so scanning it is not scanning the chrome | RPT-01; BOOK-04 | S | A-087 | Phase 11 |
| 107 | ✅ A-105 | **The public "no preference" rescue asks the room as though she were a stranger — the A-083 shape, on the caller A-083 did not reach.** `confirmAppointment` resolves the client at `public-actions.ts:364` and then, in its own `catch` at `:429`, calls `sameTimeWithSomebodyElse(...)`, which asks `anyProviderAt` with **no `holderKey`** (`:252-266`). Strict direction, so it can only ever offer FEWER times than the write would take — measured over the future book: **472 comparisons, 23 instants the named question offers that the anonymous one refuses, 0 the other way.** The concrete one: Tuesday 13:45, Marcy Dunn is in Tess's chair with her body ending at 13:45 and all four chairs' envelopes overlapping that instant — to a stranger there is no fifth chair, to her it is **the chair she is physically sitting in** (A-063, half-open bodies meeting). So she books a blow-dry straight after her cut, the stylist is taken while she types, and the rescue that exists precisely so she is not dead-ended does not fire. **A refusal handler does not read like a caller of the room's question**, which is why A-083 walked past it — A-097's rule one door on: the fallback arm is a reader too, and this one inherited the default instead of the answer it already had | BOOK-01; A-071, A-083; D-10 | S | A-083 | Phase 11 |

> **Demo checkpoint 8 — walked 2026-09-07 at the Phase 10 close, and the walk itself was clean where it mattered.** 1,609 offers with **0** lacking a chair on a room that finally binds (44 `no-resource-free` refusals, against 0 at checkpoint 7); 50 route×scheme pairs all 200, all axe-clean in **both** schemes, 0 console errors (A-096 holds); and the freed-slot loop — `/staff/opened` → waitlist match → Book — walked end to end for the first time, because A-095 gave the demo install rows to walk it with. Full transcript: `docs/reviews/19-demo-checkpoint-8.md`. **The rule it leaves behind:** an empty state must not answer a question that was never asked, and when you fix one, fix the SHAPE — grep for the zero-row branch that does not read the parameter it was filtered by, because the sibling with no spec is the one still lying at the next checkpoint.

### Phase 12 — scoped 2026-09-09 from demo checkpoint 9 and the operator review at the Phase 11 close

> `docs/reviews/20-demo-checkpoint-9.md` and `docs/reviews/20-operator-review-phase-11-close.md`. **Two reviews, and for the first time they found opposite halves of one boolean.** The operator found a parameter every caller passes identically — `audience: 'public' | 'staff'` on `daysWithAvailability` and `anyProviderDays`, four call sites, four `'public'`s — so **the customer on her phone gets a list of the days she can come and the desk on the phone with her gets `<input type="date">` and "Try another day."** on a book where 30% of (stylist, service) pairs offer nothing on a given open day. The checkpoint flipped `Provider.active` and diffed thirteen screens: A-098 holds on twelve of them, and on the thirteenth the two controls a real day needs — *"Behind by · Set"* and *"Push the column"* — are hidden from the column A-098 went to great lengths to keep drawing, under a comment claiming she is not in the building, twelve lines below a comment saying she is here because her clients are. **Neither write path checks `active`; both accept.** Rows 108–110 are the two reviews' headline findings and the notification blindness; 111–115 are the rest. **Two rows need a NEW D-number and say so (110, 114); nothing here re-opens a settled decision.**

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 108 | ✅ A-106 | **The desk cannot answer "when can you fit me in?", and the customer can.** `daysWithAvailability` (`slot-query.ts:306`, SLOT-07) and `anyProviderDays` (`any-provider.ts:240`) both take `audience: 'public' \| 'staff'` and **all four call sites pass `'public'`** — the public flow twice (`public-actions.ts:150`, `:217`), the customer's own manage link once (`manage/actions.ts:156`), and `anyProviderDays` internally. **The `'staff'` arm has been dead code since it was written.** So `/manage/{token}` renders *"Which day suits you?"* over a grid of only the days that have something, at eleven at night, unassisted — and the desk moving the identical appointment gets a bare date box and one of three dead ends: `booking-panel.tsx:416` (*"Nobody can take that on Thursday. Try another day."*), `:464` (*"She is not working that day."*), and `move-panel.tsx:108` (*"Nothing free that day for this visit. Try another day."*) — **the last of which is where `/staff/conflicts`'s per-row "Move her" link lands** (`conflict-list.tsx:98`), so the rescue for a sick stylist is the one surface that cannot say which day she is back. Measured over the next 30 days at `audience: 'staff'`: **288 of 840 (stylist, service, day) cells offer nothing; on the seven open days in the next nine, 59 of 196 = 30%; Dana's colour book is dead for eight consecutive days and the longest run for one pair is ten.** A-103 already built this answer — walk forward on the calendar axis, name the soonest following day, cap it — and scoped it to `?walkin=1`. Give all three refusals the same answer, computed **once in one server action, never per surface**: a second opinion about "when is she free" is this repo's most-found defect. **Reuse `daysWithAvailability` with `audience: 'staff'`; do not write a cheaper predicate** — SLOT-07 exists because a day list built from an approximation offers a day the booking page then refuses (checkpoint 6). Cap at a fortnight (A-103's week is right for somebody standing in the building; a phone caller is not) and **keep the date box beside it** — `move-panel.tsx:11-18` is right that "she already said next Tuesday" is a real question too. **The fixture is the item:** a MULTI-DAY absence with the stylist genuinely gone for a week, because on a book where everybody works the same hours "tomorrow" is always the answer and a search that walks one day passes (A-100's rule, third time) | BOOK-01, BOOK-04, SLOT-07; APPT-05 | M | A-103, A-100 | Phase 12 |
| 109 | ✅ A-107 | **The stylist working her notice is the one column the desk cannot tell the screen is running late.** A-098 taught six readers of `Provider.active` to each ask their own question, and on the demo book its result is unambiguous: flip one boolean and `/staff/conflicts` goes from *"Nothing stranded"* to **16 / 27 / 27 / 7** worked rows with phone numbers, her column stays drawn, her past visits stay closeable, her freed spans stay sellable, and the public site drops her. **It missed the CONTROLS on the surface it had just taught to keep drawing her.** `day-grid.tsx:159` hides `ColumnControls` — *"Behind by · Set"* and *"Push the column"* — for an off-roster column, under a comment reasoning *"she is not in the building"*, **twelve lines below the comment that says SHE IS HERE BECAUSE HER CLIENTS ARE**. On the seeded book that is **123 appointments over EIGHT working days, 2026-09-09 … 2026-09-18**. **Neither write path checks `active`** — no `active` anywhere in `running-late.ts` or `push-column.ts` — and both accept when called directly (`setRunningLate` stored 25 min; `previewPush` computed a plan). **The whole downstream chain works for her**: with the delta stored her column renders `→ likely 09:25`, D-43's `deltaAfterPush` is intact, the print sheet reads the same field. **Only the door is missing, and `ColumnControls` at `day-grid.tsx:161` is the sole inbound reference in the repo** — so a delta set before she is taken off the roster is STRANDED, with no control on any screen able to clear it, and every projected chip in her column wrong for the rest of the day. This is the **offered-then-refused class inverted**: a read model stricter than the write, refusing work the salon needs. `offRoster` currently answers *"may I seat somebody new here?"* and is being read as *"is she in the building?"* — those coincide only on the day her last appointment ends. Separate them, and **grep the day surfaces for every other control gated on the same boolean**, not just these two. `off-roster.spec.ts` asserts the marker is present and the booking link is gone; it cannot see an absent control, so the spec must assert the controls are THERE and that a stored delta can be cleared | AVAIL-05, APPT-05; D-43 | M | A-098, A-018 | Phase 12 |
| 110 | ✅ A-108 | **DECIDE FIRST (a new D-number), THEN BUILD: two ways a client is never told, and neither is visible.** `listStuckNotifications` (`stuck.ts:55`) filters `OR: [failed, pending AND attempts > 0]`, justified as *"a fresh `pending` row is not stuck, it is new"* — true **while the dispatcher is running**. A row the dispatcher has never touched is `pending` with `attempts = 0` forever and is invisible to the one screen that exists to find it; `countFailedNotifications` (`stuck.ts:91`) counts `failed` only, so the badge reads 0 beside it. Proved on **two independent databases** — `bookable_test` at the operator review and `bookable_dev` at checkpoint 9 — **713 outbox rows, all `pending`, all never attempted, and the screen says "Everything has gone out. Nothing is waiting and nothing has been given up on."**, character for character what a healthy salon sees. Separately and worse, because it is silent in production too: `reminderWindow` (`reminder.ts:21-36`) is a **5-minute band anchored to `now` with no watermark, no catch-up, and no row anywhere in the schema recording that the job ever ran** — so a deploy, a cold start, a 5xx or a rotated `CRON_SECRET` permanently loses everyone starting in that band and there is no list to work. NOTIF-02 says the window is one *tick* wide; the tick is a deployment setting and `route.ts:15-18` names a plan where it is a day — a factor of 288, sending almost nothing, with a green screen. Three parts: a **never-attempted bucket bounded by age** (*"14 messages queued over an hour ago and not tried — the job may not be running"*), the badge counting it, and a **sweep watermark** the reminder job writes so the gap is derivable. `enqueueNotification`'s `reminder-24h:{id}:{startAtEpochMs}` key (P1-7) already makes a re-sweep idempotent, which is what makes this an M. **Ride along, one line:** `sendDueReminders` (`reminders.ts:53`) is the only core query in the repo with **no `businessId` filter**. **Ride along, and the operator asked for it here rather than in the screen:** the seed enqueues 713 confirmations and dispatches none, so `/staff/messages` is the one screen a checkpoint cannot walk truthfully — **dispatch in the seed**, do not change the screen. **The decision:** what the job does about a band it missed. (a) sweep backwards to a watermark and enqueue anything still ≥ N hours out; **(b) record the sweep, DERIVE the gap, and put the missed cohort on `/staff/messages` as a named list with phone numbers** — the operator's recommendation, D-46's shape, no policy needed about what a late message may claim; (c) both. **Not in scope:** any real channel (A-053 stays blocked), and any message that makes a claim about the future after its own window has passed | NOTIF-01, NOTIF-02; D-14, D-37, D-38 | M | A-051, A-048 | Phase 12 |
| 111 | ✅ A-109 | **The screen whose whole subject is perishable money puts the unsellable row at the top.** A released no-show's span on `/staff/opened` is **live** — `freedMinutes = minutesBetween(start, span.end)` with `start` at `now` (`opened.ts:376-377`) — so it counts down through the afternoon, and **the only guard anywhere is against zero** (`opened.ts:226`). Walked: a row reading *"Wednesday 9 September at 10:54 · 10 min · Blow-dry · Marcus"* whose own "Who wants this slot?" link nine minutes later carried **`minutes=2`** and landed on *"Blow-dry with Marcus at 11:03. Nobody on the waitlist fits this one."* **The shortest thing this salon sells is 15 minutes of footprint** (`min(duration + before + after)` over active services — a Fringe trim), so the row was unsellable at 10 minutes, before it decayed to 2. Three things compound: the heading names the **whole 30-minute service** the desk then rings somebody about; `matchFreedSlot` **already applies the footprint bound correctly** one function over, so one half of the loop knows and the other half is still selling; and **"Soonest to expire first" sorts the dead row to the TOP** — a span decayed to two minutes is by construction the soonest to expire, so the ordering *guarantees* the unbuyable row occupies the position the screen reserves for the most urgent thing (confirmed: it was link 1 of 2). One predicate in the listing, the same one `matchFreedSlot` uses. **The fixture needs a span that has decayed BELOW the shortest footprint but is still positive** — at zero it already drops out, so a test written against a fresh release passes against the bug | APPT-01; A-069, A-102, A-043 | S | A-102 | Phase 12 |
| 112 | ✅ A-110 | **The waitlist has one door in — the nav — and none out.** Grepped: `apps/web/app/staff/book/`, `apps/web/app/staff/day/` and `apps/web/lib/booking/` contain **zero references to the waitlist**; `staff-nav.tsx:52` is the only inbound link besides `/staff/opened`'s sell-direction one. So the moment WAIT-01 exists for — *"Nobody can take that on Thursday"* (`booking-panel.tsx:416`, `:464`) — offers nothing, and adding her means navigating away and **searching for the client a second time** on a form (`entry-form.tsx:29-36`) whose every field was on the screen just abandoned, with her still on the phone. Put **"Put her on the list for this"** on both refusals, prefilled from what the panel already holds: client, services, the stylist or the set she would accept, the day she asked for as `fromDay`. **No second write path** — `addWaitlistEntry` unchanged, called with a prefill. Second half, and it is why the list is not read: **nothing in the repo ever writes `expired`** (grepped, zero outside the enum) and `listWaitlist` (`waitlist.ts:81`) filters on status alone, so an entry whose own `toDay` was in June sits on the standing queue in September looking exactly like somebody to ring — while `matchFreedSlot` (`waitlist.ts:181-187`) correctly refuses to match it. **Silently dead and visibly live**, and §8 of the master PRD promises the opposite in writing: *"stale entries leave the panel."* **Derive it from `toDay`, never a job** — a job is a second write path that eventually disagrees with the read (A-077's shape for the lapsed call marks). **Ride along, one line:** the "Which days" checkbox list is the seven weekdays, so a client can be waitlisted for **Sunday and Monday only** — the two days this salon is shut — and never be matched; build it from the business's open days. **The fixture needs an entry whose window is in the past AND a freed slot inside its old range**, because that is the only place the listing and the matcher disagree | WAIT-01, WAIT-02; BOOK-04 | S | A-023, A-043 | Phase 12 |
| 113 | ✅ A-111 | **The product assumes every client is a woman, on the verbs, and a quarter of this book is not.** `/staff/unfinished` is the screen the desk works at close of business — 124 rows, *"$3540.00 of work the week's figures cannot see"* — and under every one of them are two buttons reading **"She came"** and **"She didn't"** (`close-out-buttons.tsx:32`, `:44`). Two of the thirteen seeded clients are men; **Tom Byrne and Sam Okafor hold 171 of the 718 appointments (23.8%), and 33 of the 127 rows on that screen right now (26%)**. The screen renders **248 occurrences of "she"/"her"** and a quarter of them are about a man. It is not one screen: the same voice is on `status-controls.tsx:125`, `:177`, `:240`, `who-was-this.tsx:85`, `visit-panel.tsx:136`, `appointments/[id]/page.tsx:317` and `:394`, `move-panel.tsx:108`, `end-series-panel.tsx:37,82,88`, `opened/page.tsx:78`, `settings-form.tsx:98` — and, worst of the set because the desk reads it **down the phone to the person it is about**, `clients/[id]/page.tsx:96`: *"**She** cannot book online — the desk can."* The product does not store, ask for, or infer client gender and has no business asserting it. **This is the client copy only** — "her working hours" about Dana or Tess is correct and stays. Every rewrite is shorter: *Came* / *Didn't come*, *Put the time back on the book*, *This wasn't them*, *Was the client told?*. **Nothing catches it and nothing can**: it compiles, all 1,590 unit and 305 e2e tests pass, axe does not read English, and the design gallery renders the same strings and calls them correct. Sweep the whole product for second-person and third-person client copy, not the two screens that noticed — **and add the lint or the test that makes the next one fail**, because CLAUDE.md's rule is that patching the rooms that noticed leaves the door open (A-096) | CLIENT-02, CLIENT-04, APPT-01; Design brief | S | — | Phase 12 |
| 114 | ✅ A-112 | **DECIDE FIRST (a new D-number), THEN BUILD: a mis-tapped cancellation cannot be corrected, and the mark it leaves is permanent.** `transitions.ts:174-178` gives `cancelled` and `cancelled_late` **no outgoing edges at all**, reasoning that the slot was genuinely released and may already have been sold — which is the argument **D-45 already answered on the release axis** by letting the exclusion constraint decide and telling the desk in words. Three harms, rising: D-32 has already sent the wrong client a cancellation notice **by default** (the suppression box is unticked), so she gets told she is cancelled and then, after the rebooking, told she is booked; the recovery is a **different appointment** with a new id, a new manage token and an event log split across two rows, which is APPT-07's promise coming apart; and `reliability.ts:88` + `flagSentence` (`client-flag.tsx:23-29`) put *"N late cancels"* on four surfaces of her twelve-month record with **no correction path of any kind** — while the identical daily mis-tap on `no_show` has had one since D-7 precisely because *"mis-taps are daily and the alternative is SQL surgery"*. **The decision:** may a cancellation be undone, and under what guard? (a) an APPT-06-style edge `cancelled | cancelled_late → booked`, staff only, inside APPT-06's existing 7-day window, reason required, **refused by `appointment_block_no_overlap` the moment anything has been sold into the time and mapped through `errors.ts` to a sentence, never a stack trace** — the operator's recommendation and D-45's exact mechanism; (b) no reinstatement but a `cancelled_late → cancelled` downgrade so the innocent flag comes off; (c) leave it. If (a): the reinstatement must **re-point the existing manage token** (D-5's `repointManageTokens`), never mint a second, and must tell the client only if the desk says so — D-32's checkbox, defaulting the other way here because she has just been told the opposite. **A-078's rule applies:** provoke BOTH error shapes, statement-time and deferred, because a handler that requires `23P01` is dark on every deferred path | APPT-01, APPT-06, CLIENT-04; D-7, D-32, D-42, D-45 | S | A-060, A-075 | Phase 12 |
| 115 | ✅ A-113 | **The demo install cannot demonstrate the one Phase 11 item that is purely a rendering promise, and its client list is thirteen people who are all the same.** `seedDensity` produces **zero overlapping same-provider pairs** — the SQL is in the checkpoint transcript and returns 0 — so **A-099's lanes are dormant on the demo book**: the knowingly double-booked hour D-8 promises the day view will render, and that A-099 built, cannot be walked, printed, screenshotted or shown to anybody without a hand-built fixture. That is checkpoint 7's waitlist complaint one feature over, and A-095 is the precedent for fixing it in the seed rather than in a spec. Seed **one BOOK-05 override** — two clients, one stylist, one instant, a typed reason — on a day inside the moving future book, and assert **what the SCREEN says**: two chips at the same `top`, different lanes, both names legible, and the same two rows on the printed sheet. Second half, from checkpoint 9 Scene 2: the seeded client list is **eleven women, two men, and no other variation**, which is how *"She came"* survived eight demo walks and 1,895 tests — **a demo book is the only place the product's voice is audible**, and a book where everybody is the same cannot hear it. Widen the seeded names so a checkpoint reading the rendered text has something to notice. **A-045's every-column idempotence test and A-024's frozen `1290/2100` over `DEMO_WEEK` both pin this seed** — anything added runs strictly after the fixed book in the PRNG stream, as A-095 did | D-8; A-099, A-095 | S | A-099 | Phase 12 |

> **Demo checkpoint 9 — walked 2026-09-09 at the Phase 11 close.** 52 route×scheme pairs, all 200, all axe-clean in both schemes, 0 console errors, 0 bounced to login — and every row printed the URL it actually landed on, the `<h1>` and the body length, because checkpoint 8's rule is that a green run over the wrong page looks exactly like a green run. Six routes under 700 characters were re-fetched settled to prove the number was the page and not a streamed shell. A-101 verified across four weeks (`weekIsAhead` flips correctly, `booked >= worked` on every row, checkpoint 8's four-zeroes card gone); A-100 verified on the seed's real absence; A-098 verified by flipping one boolean and diffing thirteen staff routes and four public ones. Full transcript: `docs/reviews/20-demo-checkpoint-9.md`. **The rule it leaves behind:** widening who is RENDERED is not the same edit as widening who can be ACTED ON, and the second half is the one with no test — when an item makes something render that used to vanish, grep for the CONTROLS on that surface, because a control is a reader too, its filter is usually the same boolean, and a screen that renders a row it will not let you touch fails no test: the assertion everybody writes is that the row is there. **And the measurement worth keeping:** scanning axe after `emulateMedia({colorScheme:'dark'})` **without** A-096's `FREEZE` invents **18 phantom `color-contrast` nodes on `/staff/design` and 15 on `/staff/day`**, dark only, deterministically — the helper is doing work, not ceremony, and this walk hit it precisely because it runs outside the suite where the `no-restricted-imports` ban cannot reach.

### Phase 13 — scoped 2026-09-11 from demo checkpoint 10 and the operator review at the Phase 12 close

> `docs/reviews/21-demo-checkpoint-10.md` and `docs/reviews/21-operator-review-phase-12-close.md`. **Two reviews, and each found a kind of sameness the product cannot see.** The checkpoint booked online as a seeded regular, which no walk had done in ten phases. Alice Hall is blocked from booking online after three no-shows. Typing her number as `+1 512 555 0101` gets *"We can't book this one online"*. Typing `(512) 555-0101` gets *"Your appointment is confirmed"* and **a second Alice Hall with a clean record**, because `normalizePhone` gives one number three identities and the public write reuses a client only on exact characters. The desk's search, which matches digits anywhere, shows both. The operator found the same shape on the time axis. D-6 and D-53 deliberately keep a row's id through a change, and **every reader keyed on the id alone is still asking about before**. The reinstatement returns into the chair it held, which the chair picker's ordering guarantees somebody else now has. The missed-reminder list asks whether the appointment was ever reminded, not whether it was reminded for its current start. The checkpoint's other two findings are one condition each. A-106's day list and A-110's waitlist door sit behind `offered.length === 0`, so a stylist's **full** day, the case that motivated A-106, gets neither, and only a shut day does. And `/staff/messages` prints *"Everything has gone out"* over 686 rows that every appointment page correctly calls "queued". **Rows 116 and 121 need a NEW D-number each and say so (the next is D-55, and whichever is decided first takes it); nothing here re-opens a settled decision.**

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 116 | ✅ A-114 | **DECIDE FIRST (a new D-number), THEN BUILD. The website makes a second client out of a phone number written with brackets, and a client blocked from booking online books straight past CLIENT-04.** Proved through the real `/book` flow on a fresh seed. Alice Hall has 3 no-shows and her flag reads *"Cannot book online — the desk can."* Name `Alice Hall` with phone `+1 512 555 0101` gets *"We can't book this one online. Please call the salon"*. The same name with `(512) 555-0101` gets **"Your appointment is confirmed"** and a new `Client` row (`5125550101`, 0 no-shows). `/staff/clients?q=0101` then lists two Alice Halls, one flagged, and her Saturday chip carries no flag. **Why:** `normalizePhone` (`packages/core/clients/phone.ts:22-26`) keeps a leading `+`, so `+15125550101`, `15125550101` and `5125550101` are three identities, although its own header (`:16-20`) calls two of them *"the same number written two ways"*. `confirmAppointment` reuses a client only on an exact `(phone, name)` match (`public-actions.ts:396-399`) and otherwise creates one. **The name has the same fault:** `mode: 'insensitive'` folds neither accents nor Unicode form. So `Rae Nunez`, and `Núñez` typed decomposed, each create a new client, and the desk search (`clients.ts:76`) answers *"Nobody matches 'nunez'"*. The desk's PHONE search (`clients.ts:77`, digits contained) finds her under every format, **so the desk sees one client while the website makes three**: the offered-versus-accepted class, on the identity axis. **All 13 seeded phones are stored `+1…`**, the one form a US client almost never types, so on the demo install every returning client who books online the natural way is duplicated. `public-actions.ts:386-395` accepts that a differently typed NAME gets past the block, as D-17's deliberate household trade. **Nobody decided the phone format.** **The decision:** a canonical phone form (canonicalising is not validating, so the header's "not E.164 validation" can stand) with a business default country, a migration rewriting stored phones into it, and ONE folded name comparison (NFC + accent-fold) used by both the public write and the desk search. D-17's rule that a shared phone does not merge a household stays exactly as it is. **Merge (A-015) is the recovery for rows already split; say how the desk finds them.** **The fixture is the item: two different people typing.** `booking.spec.ts:71`/`:87` types `(512) 555-0101` and asserts `5125550101` back, the same literal on both sides of the equality, which proves determinism and not recognition. Write format A and match format B; write an accented name and match its ASCII and NFD forms | CLIENT-01, CLIENT-03, CLIENT-04; D-17 | M | A-015 | Phase 13 |
| 117 | ✅ A-115 | **"When can Dana fit me in?" is answered on the day she is off, and not on the day she is full.** A-106's day list and A-110's waitlist door both sit behind `answerWhenNot(offered.length === 0, …)` (`booking-panel.tsx:284`, and the branch at `:541-547`). A-042 made `offered` carry REFUSED times too, so a full day is never empty. Driven on the panel's own date box on the demo book, Dana + Colour: **Saturday 12 shows 32 times, every one refused, with no day list and no "Put them on the list for this"; Wednesday 16 is the same; Sunday 13 (shut) shows *"The next days with room: Tuesday 22 September"* and the door.** The answer is reachable only by asking about a day the salon is closed. **The move panel on the same full Saturday answers correctly** (*"Nothing free that day for this visit. The next days with room: Tuesday 15 September…"*), because it asks `found.length === 0` of bookable slots only (`move-panel.tsx:77`). That is two surfaces giving two answers to one question on the busiest shape a book has. The comment at `:280-283` reasons that a fully refused day *"still has A-042's list, an override behind each chip, and nothing to be redirected away from"*. That is true about the override, and the override is how the desk double-books (D-8); it is not an answer to a caller asking when Dana is free. Ask the question of the BOOKABLE times: `offered.every(slot => slot.reasons.length > 0)` is the refusal. Keep A-042's refused list AND the day list and door beneath it. **Why nothing caught it:** row 108 prescribed *"a MULTI-DAY absence"* as the fixture, and an absence is the one shape that returns no candidates. The measurement that motivated the row was a FULL book. **The fixture must be a full day with no absence anywhere in it**, asserted on the named arm and against the move panel's answer for the same day (they must agree). Measured cost: ~0.3 s through the panel, so no caching | BOOK-01, BOOK-04, SLOT-07, WAIT-01; D-8 | S | A-106, A-110 | Phase 13 |
| 118 | ✅ A-116 | **"Put it back on the book" is refused in the room where mis-taps happen, with a sentence that is false, and the desk's only way out rebuilds every harm D-53 closed.** D-53 lets the exclusion constraint decide whether a cancelled appointment may return, into its OWN chair, because `resourceId` survived the cancellation (`transition.ts:145-148`, which calls this *"the conservative direction, and the desk can move it afterwards"*; a cancelled appointment has no move panel). But `findFreeResource` hands out the lowest-numbered free chair (`resources.ts:130`), so the chair a cancellation frees goes to the next overlapping booking on ANY stylist. Measured by the operator on three days and three chairs: Dana late-cancelled by mis-tap, and Priya's next booking seated in the freed chair. **Dana's column is empty, two chairs are free, and the reinstatement is refused** as `SlotTaken ['overlaps-booking']`, worded *"That time has been sold to somebody else since it was cancelled… Book them in somewhere else"* (`actions.ts:87`). Doing as told books the identical instant into Chair 3, and leaves a cancellation notice sent, a second confirmation, a second token, a split log, **and the late cancel D-53 existed to remove**. Re-pick the chair inside the reinstatement's transaction with `chairForMove` (`resources.ts:180`, as at `reschedule.ts:167`, `change-services.ts:487`, `attach-client.ts:279`), preferring her own chair. That is a chooser, not a check-then-write; the constraint still defends the chosen chair. Word the two refusals apart (`transition.ts:152` collapses no-chair into `overlaps-booking`). **Ride along, read and not run:** A-075's un-release, same shape (`release-time.ts:261`). **The fixture is the item:** two or more chairs, the freed one taken by a DIFFERENT stylist, another chair free, the stylist free. On one chair the refusal is correct and the test passes against the bug. Operator review §1 (proposed there as A-114) | APPT-06, RES-02, RES-03, CLIENT-04; D-30, D-53 | S | A-112, A-034 | Phase 13 |
| 119 | ⬜ A-117 | **The one failure D-51 was written for is the one the badge cannot see, and a client reminded of the wrong time is not on the list.** The missed-reminder list is exact: the operator skipped one five-minute tick on a 43-appointment cohort, and 4 were unreminded and 4 listed. But the shell badge is `countUnsentNotifications` (`stuck.ts:173`, `layout.tsx:58`), which counts OUTBOX rows, and a missed tick writes none. So at 07:00 **the badge reads 0, the stuck list is empty and the watermark says the job ran at 06:55**, while four ten o'clocks were never reminded. `stuck.ts:161` defines the badge as *"how many NOBODY HAS BEEN TOLD ABOUT"*. **Part 1:** count the cohort in the badge from `listMissedReminders` itself, never a second predicate, and assert badge = actionable outbox rows + listed cohort on a book with a skipped tick. **Part 2:** predicate 4 (`missed-reminders.ts:76`) asks whether the APPOINTMENT holds any reminder row, while the sweep's identity is `reminder-24h:{id}:{startAtMs}` (`reminders.ts:112`). Measured: Sam Okafor, reminded for Saturday and moved to Wednesday on D-6's same row, is absent from a 41-row list, and the sweep at that band writes him a second key. Match on the key from the CURRENT `startAt` via one exported builder, and move predicate 3 (`:89`) with it: "early enough" must mean "at THIS time", i.e. the latest event that rewrote `startAt`, else `createdAt`. **Not in scope:** a catch-up enqueue (D-51's (b) stands), a cron-interval alarm. **Fixtures:** a skipped tick with every other tick run; remind, then reschedule by more than a day, then skip the new band. Operator review §2 (proposed there as A-115) | NOTIF-01, NOTIF-02; D-6, D-51 | S | A-108 | Phase 13 |
| 120 | ⬜ A-118 | **`/staff/messages` says "Everything has gone out" over messages no real driver delivered, while every appointment page correctly says "queued".** On the demo book there are 686 outbox rows, all `status = sent`, all `deliveredBy = log`. `/staff/appointments/{id}` renders each as *"queued"* via `deliveryWord` (`event-language.ts:239-245`), which asks `reallyDelivered` (`provider.ts:33`) for exactly the reason `provider.ts:14-32` gives: *"'Told: Cancellation — sent' is read at the front desk as 'no need to call her'"*. `/staff/messages`' `allClear` (`page.tsx:55`) never asks, and `:70` prints *"Everything has gone out. Nothing is waiting and nothing has been given up on."* `notificationAdapter` is `LoggingChannelAdapter` in every build (`provider.ts:12`; A-053 blocked), so **on every install that exists today, the screen headed "Messages that did not go out" says everything went out, and every appointment says it did not.** A-044/A-048 already fixed this sentence on the appointment page; A-108 rebuilt this screen and inherited the older reading, and its seed dispatch turned a blind screen into a contradicting one. Derive the healthy sentence from the SAME predicate `deliveryWord` uses, never a second copy, and say what is true when nothing was really delivered. Assert on a book of `log` rows that the two screens agree. **Not in scope:** any real channel (D-14) | NOTIF-01; D-14; A-044, A-048 | S | A-117 | Phase 13 |
| 121 | ⬜ A-119 | **DECIDE FIRST (a new D-number), THEN BUILD. The waitlist remembers one service of a two-service visit, offers her time she cannot use, and hides time she can.** A-110's refusal link carries the whole visit (`booking-panel.tsx:312`). The entry form selects the first service (`entry-form.tsx:145`) and names the rest in a sentence (`:158`, *"one service per entry, so say so when you ring them"*) that nothing stores, because `WaitlistEntry` has one `serviceId`. `matchFreedSlot` filters `serviceId: freed.serviceId` (`waitlist.ts:215`) and fits that one service. Measured by the operator: a Cut+Colour waitlisting (a 185-minute footprint under D-23) stored as `Cut` **matches a 55-minute freed cut and is not offered a 190-minute span that holds her whole visit**. The match row shows a name and a phone (`waitlist/page.tsx:112-116`); the warning was on last week's form. Cut + colour is *"half the sample business's Saturday book"* (D-23). **The decision** amends WAIT-01's definition of an entry (`00-master-prd.md:137`). (a) Ordered `serviceIds`; fit the COMPOSED footprint at the freed span's provider with D-23's end-buffers and every line qualified; offer any span she fits, whatever freed it. This is the operator's recommendation, and it also retires `opened.ts:326`'s `primaryServiceId` as the decider of who is asked. (b) As (a), but only spans freed by a shared service. (c) Keep one service and render the rest on the match row. Migration: single-service rows become one-element lists. **Ride along, one line:** the standing queue prints stored tags, *"· saturday, morning"* (`waitlist/page.tsx:200` joins `dayParts` raw); render them in the salon's words. **Fixture:** a two-service entry and three spans: shorter than the visit but longer than its first service; longer than the visit, freed by a service not hers; exactly the footprint. Operator review §3 (proposed there as A-116) | WAIT-01, WAIT-02, VISIT-01; D-23 | M | A-110, A-109 | Phase 13 |
| 122 | ⬜ A-120 | **The demo install still cannot show two things Phase 12 built, and the chip cuts off the half of the flag the desk acts on.** (1) **The missed-reminder list is empty by construction:** on a fresh seed, 31 booked appointments start in the next 24 hours, **0** were created 24 hours before they start (predicate 3), and `remindersLastRunAt` is NULL, because `seedDensity` writes the whole book at seed time. *"The reminder job has never run"* is the only trace of A-108's list on the demo book, which is A-113's lanes complaint one feature over. Give the future book booking dates in the past, and run the sweep over the elapsed ticks with ONE band deliberately skipped, stamping the watermark, the way A-108 made the seed dispatch its outbox. Anything added runs strictly after the fixed book in the PRNG stream, so A-024's frozen `1290/2100` and A-045's diff stand. (2) **The long name is kept off the day, for a reason the measurement contradicts.** `density-seed.ts:807-809` puts *Jordan Fairweather-Okonkwo* in the lapsed pool because *"a half-width chip beside an override does not"* have room. Renamed onto a live client at the checkpoint, the name is cut in the lane (157 px in 151) **and on ordinary chips in an ordinary column (157 in 144–145)**. Rename one of the eight `fill` clients IN PLACE (same count, order, phones: A-113's rule) so a long name is on the day, and measure `scrollWidth <= clientWidth` where the claim is legibility. (3) **The flag's second clause is never on the grid.** `appointment-chip.tsx:126` renders `⚑ 3 no-shows in the last 12 months. Cannot book online — the desk can.` as `block truncate`: 386 px of text in 178–185 px on every ordinary chip, while the accessible name carries it all, so axe and `getByRole` pass. Give the chip a short form that keeps the consequence. (4) **The seed log says 686 appointments and the table holds 691**: the five lapsed visits are not counted | NOTIF-02, CLIENT-04; D-54; A-095, A-108, A-113 | M | A-108, A-113 | Phase 13 |

> **Demo checkpoint 10 — walked 2026-09-11 at the Phase 12 close,** on a production build over a freshly created `bookable_cp10`. 74 route×scheme pairs, all 200, all axe-clean in both schemes, 0 console errors, 0 bounced to login, every row printing the URL it landed on. **A-111 measured from the rendered side: 0 gendered pronouns in the text of 37 routes**, including `/staff/unfinished`, where checkpoint 9 counted 248. A-113's override renders as D-54 promised: span 2, the chips side by side at one `top`, both names fitting, both sheet rows saying *"At the same time as"*. A-107 was verified by flipping Tess off the roster (the controls stay). A-112's undo is offered inside the window and refused outside it. Full transcript: `docs/reviews/21-demo-checkpoint-10.md`. **The rule it leaves behind:** a fixture that makes a case exist usually makes the version of it that works. A-106's row measured a full book and prescribed an absence. A-113 placed the long name where it fits. `booking.spec.ts` recognises a client by typing her number once and asserting it back. When a row carries a measurement, the fixture must reproduce the measurement's shape, and an equality between two things two people type needs two people typing.
